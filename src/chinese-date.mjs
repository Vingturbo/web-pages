// Copyright (c) 2025 北京锋通科技有限公司 (郭玉峰, 吴琼). MIT License.
// VSOP87B Earth truncated data (threshold 1e-7, 220 terms retained)
var vsop87BearthTruncated = { "type": "B", "L": { "0": [[1.75347045673, 0, 0], [0.03341656453, 4.66925680415, 6283.0758499914], [0.00034894275, 4.62610242189, 12566.1516999828], [0.00003417572, 2.82886579754, 3.523118349], [0.00003497056, 2.74411783405, 5753.3848848968], [0.00003135899, 3.62767041756, 77713.7714681205], [0.00002676218, 4.41808345438, 7860.4193924392], [0.00002342691, 6.13516214446, 3930.2096962196], [0.00001273165, 2.03709657878, 529.6909650946], [0.00001324294, 0.74246341673, 11506.7697697936], [0.00000901854, 2.04505446477, 26.2983197998], [0.00001199167, 1.10962946234, 1577.3435424478], [0.00000857223, 3.50849152283, 398.1490034082], [0.00000779786, 1.17882681962, 5223.6939198022], [0.0000099025, 5.23268072088, 5884.9268465832], [0.00000753141, 2.53339052847, 5507.5532386674], [0.00000505267, 4.58292599973, 18849.2275499742], [0.00000492392, 4.20505711826, 775.522611324], [0.00000356672, 2.91954114478, 0.0673103028], [0.00000284125, 1.89869240932, 796.2980068164], [0.00000242879, 0.34481445893, 5486.777843175], [0.00000317087, 5.84901948512, 11790.6290886588], [0.00000271112, 0.31486255375, 10977.078804699], [0.00000206217, 4.80646631478, 2544.3144198834], [0.00000205478, 1.86953770281, 5573.1428014331], [0.00000202318, 2.45767790232, 6069.7767545534], [0.00000126225, 1.08295459501, 20.7753954924], [0.00000155516, 0.83306084617, 213.299095438], [0.00000115132, 0.64544911683, 0.9803210682], [0.00000102851, 0.63599845579, 4694.0029547076], [0.00000101724, 4.2667980198, 7.1135470008], [9.9206e-7, 6.20992926918, 2146.1654164752], [0.00000132212, 3.41118292683, 2942.4634232916], [9.7607e-7, 0.68101342359, 155.4203994342], [8.5128e-7, 1.29870764804, 6275.9623029906], [7.4651e-7, 1.755089133, 5088.6288397668], [0.00000101895, 0.97569280312, 15720.8387848784], [8.4711e-7, 3.67080093031, 71430.69561812909], [7.3547e-7, 4.67926633877, 801.8209311238], [7.3874e-7, 3.50319414955, 3154.6870848956], [7.8757e-7, 3.03697458703, 12036.4607348882], [7.9637e-7, 1.80791287082, 17260.1546546904], [8.5803e-7, 5.9832263126, 161000.6857376741], [5.6963e-7, 2.78430458592, 6286.5989683404], [6.1148e-7, 1.81839892984, 7084.8967811152], [6.9627e-7, 0.83297621398, 9437.762934887], [5.6116e-7, 4.38694865354, 14143.4952424306], [6.2449e-7, 3.97763912806, 8827.3902698748], [5.1145e-7, 0.28306832879, 5856.4776591154], [5.5577e-7, 3.47006059924, 6279.5527316424], [4.1036e-7, 5.36817592855, 8429.2412664666], [5.1605e-7, 1.33282739866, 1748.016413067], [5.1992e-7, 0.18914947184, 12139.5535091068], [4.9e-7, 0.48735014197, 1194.4470102246], [3.92e-7, 6.16833020996, 10447.3878396044], [3.557e-7, 1.775968892, 6812.766815086], [3.677e-7, 6.04133863162, 10213.285546211], [3.6596e-7, 2.56957481827, 1059.3819301892], [3.3296e-7, 0.59310278598, 17789.845619785], [3.5954e-7, 1.70875808777, 2352.8661537718], [4.0938e-7, 2.39850938714, 19651.048481098], [3.0047e-7, 2.73975124088, 1349.8674096588], [3.0412e-7, 0.44294464169, 83996.84731811189], [2.3663e-7, 0.48473622521, 8031.0922630584], [2.3574e-7, 2.06528133162, 3340.6124266998], [2.1089e-7, 4.14825468851, 951.7184062506], [2.4738e-7, 0.21484762138, 3.5904286518], [2.5352e-7, 3.16470891653, 4690.4798363586], [2.2823e-7, 5.22195230819, 4705.7323075436], [2.1419e-7, 1.42563910473, 16730.4636895958], [2.1891e-7, 5.55594302779, 553.5694028424], [1.7481e-7, 4.56052900312, 135.0650800354], [1.9927e-7, 5.22209149316, 12168.0026965746], [1.986e-7, 5.77470242235, 6309.3741697912], [2.03e-7, 0.37133792946, 283.8593188652], [1.4421e-7, 4.19315052005, 242.728603974], [1.6225e-7, 5.98837767951, 11769.8536931664], [1.5077e-7, 4.1956716337, 6256.7775301916], [1.9124e-7, 3.82219958698, 23581.2581773176], [1.8888e-7, 5.38626892076, 149854.4001348079], [1.4346e-7, 3.72355084422, 38.0276726358], [1.7898e-7, 2.21490566029, 13367.9726311066], [1.2054e-7, 2.62229602614, 955.5997416086], [1.1287e-7, 0.17739329984, 4164.311989613], [1.3973e-7, 4.40134615007, 6681.2248533996], [1.3621e-7, 1.88934516495, 7632.9432596502], [1.2503e-7, 1.13052412208, 5.5229243074], [1.0498e-7, 5.35909979317, 1592.5960136328], [1.0327e-7, 6.19982170609, 6438.4962494256], [1.2003e-7, 1.00351462266, 632.7837393132], [1.0827e-7, 0.32734523824, 103.0927742186], [1.0005e-7, 6.0291496328, 5746.271337896], [1.0523e-7, 0.93870455544, 11926.2544136688]], "1": [[6283.0758499914, 0, 0], [0.00206058863, 2.67823455808, 6283.0758499914], [0.00004303419, 2.63512233481, 12566.1516999828], [0.00000425264, 1.59046982018, 3.523118349], [0.00000109017, 2.96631010675, 1577.3435424478], [9.3479e-7, 2.59211109542, 18849.2275499742], [0.00000119305, 5.79555765566, 26.2983197998], [7.2121e-7, 1.13840581212, 529.6909650946], [6.7784e-7, 1.87453300345, 398.1490034082], [6.735e-7, 4.40932832004, 5507.5532386674], [5.9045e-7, 2.88815790631, 5223.6939198022], [5.5976e-7, 2.17471740035, 155.4203994342], [4.5411e-7, 0.39799502896, 796.2980068164], [3.6298e-7, 0.46875437227, 775.522611324], [2.8962e-7, 2.64732254645, 7.1135470008], [1.9097e-7, 1.84628376049, 5486.777843175], [2.0844e-7, 5.34138275149, 0.9803210682], [1.8508e-7, 4.96855179468, 213.299095438], [1.6233e-7, 0.03216587315, 2544.3144198834], [1.7293e-7, 2.9911676063, 6275.9623029906], [1.5832e-7, 1.43049301283, 2146.1654164752], [1.4608e-7, 1.2046979369, 10977.078804699], [1.1877e-7, 3.25805082007, 5088.6288397668], [1.1514e-7, 2.07502080082, 4694.0029547076], [1.2461e-7, 2.83432282119, 1748.016413067], [1.1808e-7, 5.27379760438, 1194.4470102246], [1.0641e-7, 0.76614722966, 553.5694028424]], "2": [[0.00008721859, 1.07253635559, 6283.0758499914], [0.0000099099, 3.14159265359, 0], [0.00000294833, 0.43717350256, 12566.1516999828], [2.7338e-7, 0.05295636147, 3.523118349], [1.6333e-7, 5.18820215724, 26.2983197998], [1.5745e-7, 3.68504712183, 155.4203994342]], "3": [[0.00000289058, 5.84173149732, 6283.0758499914], [2.0712e-7, 6.0498393902, 12566.1516999828]], "4": [], "5": [] }, "B": { "0": [[0.0000027962, 3.19870156017, 84334.66158130829], [0.00000101643, 5.42248619256, 5507.5532386674], [8.0445e-7, 3.88013204458, 5223.6939198022], [4.3806e-7, 3.70444689759, 2352.8661537718], [3.1933e-7, 4.00026369781, 1577.3435424478], [2.2724e-7, 3.9847383156, 1047.7473117547], [1.6392e-7, 3.56456119782, 5856.4776591154], [1.8141e-7, 4.98367470262, 6283.0758499914], [1.4443e-7, 3.70275614915, 9437.762934887], [1.4304e-7, 3.41117857526, 10213.285546211], [1.1246e-7, 4.82820690527, 14143.4952424306], [1.09e-7, 2.08574562329, 6812.766815086], [1.0367e-7, 4.05663927945, 71092.88135493269]], "1": [[0.00227777722, 3.4137662053, 6283.0758499914], [0.00003805678, 3.37063423795, 12566.1516999828], [0.00003619589, 0, 0], [7.1542e-7, 3.32777549735, 18849.2275499742]], "2": [[0.00009721424, 5.1519280992, 6283.0758499914], [0.00000233002, 3.14159265359, 0], [0.00000134188, 0.64406212977, 12566.1516999828]], "3": [[0.00000275993, 0.59480097092, 6283.0758499914], [1.7034e-7, 3.14159265359, 0]], "4": [], "5": [] }, "R": { "0": [[1.00013988784, 0, 0], [0.01670699632, 3.09846350258, 6283.0758499914], [0.00013956024, 3.05524609456, 12566.1516999828], [0.0000308372, 5.19846674381, 77713.7714681205], [0.00001628463, 1.17387558054, 5753.3848848968], [0.00001575572, 2.84685214877, 7860.4193924392], [0.00000924799, 5.45292236722, 11506.7697697936], [0.00000542439, 4.56409151453, 3930.2096962196], [0.0000047211, 3.66100022149, 5884.9268465832], [0.0000032878, 5.89983686142, 5223.6939198022], [0.00000345969, 0.96368627272, 5507.5532386674], [0.00000306784, 0.29867139512, 5573.1428014331], [0.00000174844, 3.01193636733, 18849.2275499742], [0.00000243181, 4.2734953079, 11790.6290886588], [0.00000211836, 5.84714461348, 1577.3435424478], [0.0000018574, 5.02199710705, 10977.078804699], [0.00000109835, 5.0551063586, 5486.777843175], [9.8316e-7, 0.88681311278, 6069.7767545534], [8.65e-7, 5.68956418946, 15720.8387848784], [8.5831e-7, 1.27079125277, 161000.6857376741], [6.2917e-7, 0.92177053978, 529.6909650946], [5.7056e-7, 2.01374292245, 83996.84731811189], [6.4908e-7, 0.27251341435, 17260.1546546904], [4.9384e-7, 3.24501240359, 2544.3144198834], [5.5736e-7, 5.2415979917, 71430.69561812909], [4.252e-7, 6.01110257982, 6275.9623029906], [4.6966e-7, 2.57799853213, 775.522611324], [3.8963e-7, 5.36063832897, 4694.0029547076], [4.4666e-7, 5.53715663816, 9437.762934887], [3.5661e-7, 1.67447135798, 12036.4607348882], [3.1922e-7, 0.18368299942, 5088.6288397668], [3.1846e-7, 1.77775642078, 398.1490034082], [3.3193e-7, 0.24370221704, 7084.8967811152], [3.8245e-7, 2.39255343973, 8827.3902698748], [2.8468e-7, 1.21344887533, 6286.5989683404], [3.7486e-7, 0.82961281844, 19651.048481098], [3.6957e-7, 4.90107587287, 12139.5535091068], [3.4537e-7, 1.84270693281, 2942.4634232916], [2.6275e-7, 4.58896863104, 10447.3878396044], [2.4596e-7, 3.78660838036, 8429.2412664666], [2.3587e-7, 0.26866098169, 796.2980068164], [2.7795e-7, 1.89934427832, 6279.5527316424], [2.3927e-7, 4.99598548145, 5856.4776591154], [2.0345e-7, 4.65282190725, 2146.1654164752], [2.3287e-7, 2.80783632869, 14143.4952424306], [2.2099e-7, 1.95002636847, 3154.6870848956], [1.9509e-7, 5.38233922479, 2352.8661537718], [1.7958e-7, 0.1987136996, 6812.766815086], [1.7178e-7, 4.43322156854, 10213.285546211], [1.619e-7, 5.23159323213, 17789.845619785], [1.7315e-7, 6.15224075188, 16730.4636895958], [1.3814e-7, 5.18962074032, 8031.0922630584], [1.8834e-7, 0.67280058021, 149854.4001348079], [1.833e-7, 2.25348717053, 23581.2581773176], [1.3639e-7, 3.68511810757, 4705.7323075436], [1.3142e-7, 0.65267698994, 13367.9726311066], [1.0414e-7, 4.33285688501, 11769.8536931664], [1.017e-7, 1.59366684542, 4690.4798363586]], "1": [[0.00103018607, 1.10748968172, 6283.0758499914], [0.00001721238, 1.06442300386, 12566.1516999828], [0.00000702217, 3.14159265359, 0], [3.2345e-7, 1.02168583254, 18849.2275499742], [3.0801e-7, 2.84358443952, 5507.5532386674], [2.4978e-7, 1.31906570344, 5223.6939198022], [1.8487e-7, 1.42428709076, 1577.3435424478], [1.0077e-7, 5.91385248388, 10977.078804699]], "2": [[0.00004359385, 5.78455133808, 6283.0758499914], [0.00000123633, 5.57935427994, 12566.1516999828], [1.2342e-7, 3.14159265359, 0]], "3": [[0.00000144595, 4.27319433901, 6283.0758499914]], "4": [], "5": [] } };

/**
 * Lightweight astronomy module — replaces astronomia with a minimal implementation
 * Copyright (c) 2025 北京锋通科技有限公司 (郭玉峰, 吴琼). MIT License.
 *
 * Only includes the functions needed for Chinese lunar calendar computation:
 * - Julian calendar conversions
 * - VSOP87 truncated Earth position (for solar longitude)
 * - Nutation
 * - Solar coordinates (apparent longitude via VSOP87)
 * - Solstice/equinox computation
 * - Moon phase (new moon)
 * - DeltaT
 *
 * VSOP87 data is truncated at threshold 1e-7, reducing data from 124KB to ~10KB.
 * This provides ~10 arcsecond precision, more than sufficient for
 * determining lunar calendar dates (which only need ~1 minute accuracy).
 */
// ============================================================
// Constants
// ============================================================
const J2000 = 2451545.0;
const JulianCentury = 36525;
const BesselianYear = 365.2421988;
const D2R = Math.PI / 180;
// ============================================================
// Utility functions
// ============================================================
function horner(x, ...c) {
    if (Array.isArray(c[0]))
        c = c[0];
    let i = c.length - 1;
    let y = c[i];
    while (i > 0) {
        i--;
        y = y * x + c[i];
    }
    return y;
}
function pmod(x, y) {
    let r = x % y;
    if (r < 0)
        r += y;
    return r;
}
function sincos(ε) {
    return [Math.sin(ε), Math.cos(ε)];
}
function J2000Century(jde) {
    return (jde - J2000) / JulianCentury;
}
function JDEToJulianYear(jde) {
    return 2000 + (jde - J2000) / 365.25;
}
function floorDiv(x, y) {
    return Math.floor(x / y);
}
const int = Math.trunc;
function CalendarToJD(y, m, d, isJulian) {
    let b = 0;
    if (m < 3) {
        y--;
        m += 12;
    }
    {
        const a = floorDiv(y, 100);
        b = 2 - a + floorDiv(a, 4);
    }
    return (floorDiv(36525 * (int(y + 4716)), 100)) +
        (floorDiv(306 * (m + 1), 10) + b) + d - 1524.5;
}
function JDToCalendar(jd, isJulian) {
    const [z, f] = modf(jd + 0.5);
    let a = z;
    {
        const α = floorDiv(z * 100 - 186721625, 3652425);
        a = z + 1 + α - floorDiv(α, 4);
    }
    const b = a + 1524;
    const c = floorDiv(b * 100 - 12210, 36525);
    const d = floorDiv(36525 * c, 100);
    const e = int(floorDiv((b - d) * 1e4, 306001));
    const day = (int(b - d) - floorDiv(306001 * e, 1e4)) + f;
    const month = (e === 14 || e === 15) ? e - 13 : e - 1;
    const year = (month < 3) ? int(c) - 4715 : int(c) - 4716;
    return { year, month, day };
}
function modf(float) {
    const i = Math.trunc(float);
    const f = Math.abs(float - i);
    return [i, f];
}
function LeapYearGregorian(y) {
    return (y % 4 === 0 && y % 100 !== 0) || y % 400 === 0;
}
// Cumulative days before each month (1-indexed, non-leap year)
const DAYS_OF_YEAR = [0, 0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334];
function dayOfYear(y, m, d) {
    const leap = LeapYearGregorian(y);
    const k = (leap && m > 1) ? 1 : 0;
    return k + DAYS_OF_YEAR[m] + int(d);
}
class CalendarGregorian {
    constructor(year, month = 1, day = 1) {
        if (year instanceof Date) {
            this.fromDate(year);
        }
        else {
            this.year = year ?? 2000;
            this.month = month;
            this.day = day;
        }
    }
    toJD() {
        return CalendarToJD(this.year, this.month, this.day);
    }
    fromJD(jd) {
        const { year, month, day } = JDToCalendar(jd);
        this.year = year;
        this.month = month;
        this.day = day;
        return this;
    }
    fromJDE(jde) {
        this.fromJD(jde);
        const dT = deltaT(this.toYear());
        this.day -= dT / 86400;
        return this;
    }
    toJDE() {
        const dT = deltaT(this.toYear());
        this.day += dT / 86400;
        return this.toJD();
    }
    fromDate(date) {
        this.year = date.getUTCFullYear();
        this.month = date.getUTCMonth() + 1;
        const day = date.getUTCDate();
        const hour = date.getUTCHours();
        const minute = date.getUTCMinutes();
        const second = date.getUTCSeconds();
        const ms = date.getMilliseconds();
        this.day = day + (hour + ((minute + ((second + ms / 1000) / 60)) / 60)) / 24;
        return this;
    }
    toDate() {
        const [day, fhour] = modf(this.day);
        const [hour, fminute] = modf(fhour * 24);
        const [minute, fsecond] = modf(fminute * 60);
        const [second, fms] = modf(fsecond * 60);
        const date = new Date(Date.UTC(this.year, this.month - 1, day, hour, minute, second, Math.round(fms * 1000)));
        date.setUTCFullYear(this.year);
        return date;
    }
    toYear() {
        const [, f] = modf(this.day);
        const n = dayOfYear(this.year, this.month, Math.floor(this.day)) - 1 + f;
        const days = LeapYearGregorian(this.year) ? 366 : 365;
        return this.year + (n / days);
    }
}
// ============================================================
// DeltaT
// ============================================================
// Simplified polynomial approximations for ΔT (in seconds)
// From https://eclipse.gsfc.nasa.gov/SEcat5/deltatpoly.html
function deltaT(dyear) {
    if (dyear < -500) {
        return horner((dyear - 1820) * 0.01, -20, 0, 32);
    }
    else if (dyear < 500) {
        return horner(dyear * 0.01, 10583.6, -1014.41, 33.78311, -5.952053, -0.1798452, 0.022174192, 0.0090316521);
    }
    else if (dyear < 1600) {
        return horner((dyear - 1000) * 0.01, 1574.2, -556.01, 71.23472, 0.319781, -0.8503463, -5050998e-9, 0.0083572073);
    }
    else if (dyear < 1700) {
        return horner(dyear - 1600, 120, -0.9808, -0.01532, 1 / 7129);
    }
    else if (dyear < 1800) {
        return horner(dyear - 1700, 8.83, 0.1603, -59285e-7, 0.00013336, -1.745e-7);
    }
    else if (dyear < 1860) {
        return horner(dyear - 1800, 13.72, -0.332447, 0.0068612, 0.0041115, -37436e-8, 1.225e-5, -17e-8);
    }
    else if (dyear < 1900) {
        return horner(dyear - 1860, 7.62, 0.5737, -0.251754, 0.01680668, -4473624e-10, 2.381e-6);
    }
    else if (dyear < 1920) {
        return horner(dyear - 1900, -2.79, 1.494119, -0.0598939, 0.0061966, -197e-6);
    }
    else if (dyear < 1941) {
        return horner(dyear - 1920, 21.20, 0.84493, -0.0761, 0.0020936);
    }
    else if (dyear < 1961) {
        return horner(dyear - 1950, 29.07, 0.407, -1 / 2338, 1.0 / 2547);
    }
    else if (dyear < 1986) {
        return horner(dyear - 1975, 45.45, 1.067, -1 / 260, -1 / 718);
    }
    else if (dyear < 2005) {
        return horner(dyear - 2000, 63.86, 0.3345, -0.060374, 0.0017275, 0.000651814);
    }
    else if (dyear < 2050) {
        return horner((dyear - 2000) / 100, 62.92, 32.217, 55.89);
    }
    else if (dyear < 2150) {
        return horner((dyear - 1820) / 100, -205.72, 56.28, 32);
    }
    else {
        const u = (dyear - 1820) / 100;
        return -20 + 32 * u * u;
    }
}
// ============================================================
// Nutation (IAU 1980, truncated)
// ============================================================
function nutation(jde) {
    const T = J2000Century(jde);
    const D = horner(T, 297.85036, 445267.11148, -19142e-7, 1.0 / 189474) * D2R;
    const M = horner(T, 357.52772, 35999.050340, -1603e-7, -1 / 300000) * D2R;
    const N = horner(T, 134.96298, 477198.867398, 0.0086972, 1.0 / 56250) * D2R;
    const F = horner(T, 93.27191, 483202.017538, -36825e-7, 1.0 / 327270) * D2R;
    const Ω = horner(T, 125.04452, -1934.136261, 0.0020708, 1.0 / 450000) * D2R;
    let Δψ = 0;
    let Δε = 0;
    for (let i = table22A.length - 1; i >= 0; i--) {
        const row = table22A[i];
        const arg = row.d * D + row.m * M + row.n * N + row.f * F + row.ω * Ω;
        const [s, c] = sincos(arg);
        Δψ += s * (row.s0 + row.s1 * T);
        Δε += c * (row.c0 + row.c1 * T);
    }
    Δψ *= 0.0001 / 3600 * D2R;
    Δε *= 0.0001 / 3600 * D2R;
    return [Δψ, Δε];
}
// Nutation table 22.A (63 rows, same as astronomia)
const table22A = [
    { d: 0, m: 0, n: 0, f: 0, ω: 1, s0: -171996, s1: -174.2, c0: 92025, c1: 8.9 },
    { d: -2, m: 0, n: 0, f: 2, ω: 2, s0: -13187, s1: -1.6, c0: 5736, c1: -3.1 },
    { d: 0, m: 0, n: 0, f: 2, ω: 2, s0: -2274, s1: -0.2, c0: 977, c1: -0.5 },
    { d: 0, m: 0, n: 0, f: 0, ω: 2, s0: 2062, s1: 0.2, c0: -895, c1: 0.5 },
    { d: 0, m: 1, n: 0, f: 0, ω: 0, s0: 1426, s1: -3.4, c0: 54, c1: -0.1 },
    { d: 0, m: 0, n: 1, f: 0, ω: 0, s0: 712, s1: 0.1, c0: -7, c1: 0 },
    { d: -2, m: 1, n: 0, f: 2, ω: 2, s0: -517, s1: 1.2, c0: 224, c1: -0.6 },
    { d: 0, m: 0, n: 0, f: 2, ω: 1, s0: -386, s1: -0.4, c0: 200, c1: 0 },
    { d: 0, m: 0, n: 1, f: 2, ω: 2, s0: -301, s1: 0, c0: 129, c1: -0.1 },
    { d: -2, m: -1, n: 0, f: 2, ω: 2, s0: 217, s1: -0.5, c0: -95, c1: 0.3 },
    { d: -2, m: 0, n: 1, f: 0, ω: 0, s0: -158, s1: 0, c0: 0, c1: 0 },
    { d: -2, m: 0, n: 0, f: 2, ω: 1, s0: 129, s1: 0.1, c0: -70, c1: 0 },
    { d: 0, m: 0, n: -1, f: 2, ω: 2, s0: 123, s1: 0, c0: -53, c1: 0 },
    { d: 2, m: 0, n: 0, f: 0, ω: 0, s0: 63, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 0, n: 1, f: 0, ω: 1, s0: 63, s1: 0.1, c0: -33, c1: 0 },
    { d: 2, m: 0, n: -1, f: 2, ω: 2, s0: -59, s1: 0, c0: 26, c1: 0 },
    { d: 0, m: 0, n: -1, f: 0, ω: 1, s0: -58, s1: -0.1, c0: 32, c1: 0 },
    { d: 0, m: 0, n: 1, f: 2, ω: 1, s0: -51, s1: 0, c0: 27, c1: 0 },
    { d: -2, m: 0, n: 2, f: 0, ω: 0, s0: 48, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 0, n: -2, f: 2, ω: 1, s0: 46, s1: 0, c0: -24, c1: 0 },
    { d: 2, m: 0, n: 0, f: 2, ω: 2, s0: -38, s1: 0, c0: 16, c1: 0 },
    { d: 0, m: 0, n: 2, f: 2, ω: 2, s0: -31, s1: 0, c0: 13, c1: 0 },
    { d: 0, m: 0, n: 2, f: 0, ω: 0, s0: 29, s1: 0, c0: 0, c1: 0 },
    { d: -2, m: 0, n: 1, f: 2, ω: 2, s0: 29, s1: 0, c0: -12, c1: 0 },
    { d: 0, m: 0, n: 0, f: 2, ω: 0, s0: 26, s1: 0, c0: 0, c1: 0 },
    { d: -2, m: 0, n: 0, f: 2, ω: 0, s0: -22, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 0, n: -1, f: 2, ω: 1, s0: 21, s1: 0, c0: -10, c1: 0 },
    { d: 0, m: 2, n: 0, f: 0, ω: 0, s0: 17, s1: -0.1, c0: 0, c1: 0 },
    { d: 2, m: 0, n: -1, f: 0, ω: 1, s0: 16, s1: 0, c0: -8, c1: 0 },
    { d: -2, m: 2, n: 0, f: 2, ω: 2, s0: -16, s1: 0.1, c0: 7, c1: 0 },
    { d: 0, m: 1, n: 0, f: 0, ω: 1, s0: -15, s1: 0, c0: 9, c1: 0 },
    { d: -2, m: 0, n: 1, f: 0, ω: 1, s0: -13, s1: 0, c0: 7, c1: 0 },
    { d: 0, m: -1, n: 0, f: 0, ω: 1, s0: -12, s1: 0, c0: 6, c1: 0 },
    { d: 0, m: 0, n: 2, f: -2, ω: 0, s0: 11, s1: 0, c0: 0, c1: 0 },
    { d: 2, m: 0, n: -1, f: 2, ω: 1, s0: -10, s1: 0, c0: 5, c1: 0 },
    { d: 2, m: 0, n: 1, f: 2, ω: 2, s0: -8, s1: 0, c0: 3, c1: 0 },
    { d: 0, m: 1, n: 0, f: 2, ω: 2, s0: 7, s1: 0, c0: -3, c1: 0 },
    { d: -2, m: 1, n: 1, f: 0, ω: 0, s0: -7, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: -1, n: 0, f: 2, ω: 2, s0: -7, s1: 0, c0: 3, c1: 0 },
    { d: 2, m: 0, n: 0, f: 2, ω: 1, s0: -7, s1: 0, c0: 3, c1: 0 },
    { d: 2, m: 0, n: 1, f: 0, ω: 0, s0: 6, s1: 0, c0: 0, c1: 0 },
    { d: -2, m: 0, n: 2, f: 2, ω: 2, s0: 6, s1: 0, c0: -3, c1: 0 },
    { d: -2, m: 0, n: 1, f: 2, ω: 1, s0: 6, s1: 0, c0: -3, c1: 0 },
    { d: 2, m: 0, n: -2, f: 0, ω: 1, s0: -6, s1: 0, c0: 3, c1: 0 },
    { d: 2, m: 0, n: 0, f: 0, ω: 1, s0: -6, s1: 0, c0: 3, c1: 0 },
    { d: 0, m: -1, n: 1, f: 0, ω: 0, s0: 5, s1: 0, c0: 0, c1: 0 },
    { d: -2, m: -1, n: 0, f: 2, ω: 1, s0: -5, s1: 0, c0: 3, c1: 0 },
    { d: -2, m: 0, n: 0, f: 0, ω: 1, s0: -5, s1: 0, c0: 3, c1: 0 },
    { d: 0, m: 0, n: 2, f: 2, ω: 1, s0: -5, s1: 0, c0: 3, c1: 0 },
    { d: -2, m: 0, n: 2, f: 0, ω: 1, s0: 4, s1: 0, c0: 0, c1: 0 },
    { d: -2, m: 1, n: 0, f: 2, ω: 1, s0: 4, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 0, n: 1, f: -2, ω: 0, s0: 4, s1: 0, c0: 0, c1: 0 },
    { d: -1, m: 0, n: 1, f: 0, ω: 0, s0: -4, s1: 0, c0: 0, c1: 0 },
    { d: -2, m: 1, n: 0, f: 0, ω: 0, s0: -4, s1: 0, c0: 0, c1: 0 },
    { d: 1, m: 0, n: 0, f: 0, ω: 0, s0: -4, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 0, n: 1, f: 2, ω: 0, s0: 3, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 0, n: -2, f: 2, ω: 2, s0: -3, s1: 0, c0: 0, c1: 0 },
    { d: -1, m: -1, n: 1, f: 0, ω: 0, s0: -3, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 1, n: 1, f: 0, ω: 0, s0: -3, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: -1, n: 1, f: 2, ω: 2, s0: -3, s1: 0, c0: 0, c1: 0 },
    { d: 2, m: -1, n: -1, f: 2, ω: 2, s0: -3, s1: 0, c0: 0, c1: 0 },
    { d: 0, m: 0, n: 3, f: 2, ω: 2, s0: -3, s1: 0, c0: 0, c1: 0 },
    { d: 2, m: -1, n: 0, f: 2, ω: 2, s0: -3, s1: 0, c0: 0, c1: 0 }
];
// ============================================================
// VSOP87 Planet Position (truncated)
// ============================================================
function vsop87Sum(t, series) {
    const coeffs = [];
    const keys = Object.keys(series).sort((a, b) => Number(a) - Number(b));
    for (const x of keys) {
        const idx = Number(x);
        coeffs[idx] = 0;
        for (let y = series[x].length - 1; y >= 0; y--) {
            const term = series[x][y];
            coeffs[idx] += term[0] * Math.cos(term[1] + term[2] * t);
        }
    }
    return horner(t, ...coeffs);
}
class Planet {
    constructor(planet) {
        this.name = planet.name;
        this.type = planet.type || 'B';
        this.series = planet;
    }
    position2000(jde) {
        const T = J2000Century(jde);
        const τ = T * 0.1;
        const lon = pmod(vsop87Sum(τ, this.series.L), 2 * Math.PI);
        const lat = vsop87Sum(τ, this.series.B);
        const range = vsop87Sum(τ, this.series.R);
        return { lon, lat, range };
    }
    position(jde) {
        const T = J2000Century(jde);
        const τ = T * 0.1;
        const lon = pmod(vsop87Sum(τ, this.series.L), 2 * Math.PI);
        const lat = vsop87Sum(τ, this.series.B);
        const range = vsop87Sum(τ, this.series.R);
        if (this.type === 'B') {
            // Precess from J2000 to equinox of date
            const eclFrom = new Ecliptic(lon, lat);
            const epochFrom = 2000.0;
            const epochTo = JDEToJulianYear(jde);
            const eclTo = eclipticPosition(eclFrom, epochFrom, epochTo);
            return { lon: eclTo.lon, lat: eclTo.lat, range };
        }
        return { lon, lat, range };
    }
}
// ============================================================
// Ecliptic coordinate & Precession (minimal)
// ============================================================
class Ecliptic {
    constructor(lon, lat) {
        this.lon = lon;
        this.lat = lat;
    }
    toEquatorial(ε) {
        const [εsin, εcos] = sincos(ε);
        const [sβ, cβ] = sincos(this.lat);
        const [sλ, cλ] = sincos(this.lon);
        let ra = Math.atan2(sλ * εcos - (sβ / cβ) * εsin, cλ);
        if (ra < 0)
            ra += 2 * Math.PI;
        const dec = Math.asin(sβ * εcos + cβ * εsin * sλ);
        return { ra, dec };
    }
}
// Simplified ecliptic precession (21.2/21.3 from Meeus)
function eclipticPosition(eclFrom, epochFrom, epochTo) {
    const d = Math.PI / 180;
    const s = d / 3600;
    let ηCoeff, πCoeff, pCoeff;
    const ηt = [47.0029 * s, -0.03302 * s, 0.000060 * s];
    const πt = [174.876384 * d, -869.8089 * s, 0.03536 * s];
    const pt = [5029.0966 * s, 1.11113 * s, -6e-6 * s];
    {
        ηCoeff = ηt;
        πCoeff = πt;
        pCoeff = pt;
    }
    const t = (epochTo - epochFrom) * 0.01;
    const π = horner(t, ...πCoeff);
    const p = horner(t, ...pCoeff) * t;
    const η = horner(t, ...ηCoeff) * t;
    const sη = Math.sin(η);
    const cη = Math.cos(η);
    const [sβ, cβ] = sincos(eclFrom.lat);
    const [sd, cd] = sincos(π - eclFrom.lon);
    const A = cη * cβ * sd - sη * sβ;
    const B = cβ * cd;
    const C = cη * sβ + sη * cβ * sd;
    const eclTo = new Ecliptic(p + π - Math.atan2(A, B), 0);
    eclTo.lat = C < 0.999996 ? Math.asin(C) : Math.acos(Math.hypot(A, B));
    return eclTo;
}
// ============================================================
// Solar coordinates (VSOP87 based)
// ============================================================
const earth = new Planet(vsop87BearthTruncated);
function trueVSOP87(jde) {
    const { lon, lat, range } = earth.position(jde);
    const s = lon + Math.PI;
    // FK5 correction
    const λp = horner(J2000Century(jde), s, -1.397 * D2R, -31e-5 * D2R);
    const [sλp, cλp] = sincos(λp);
    const Δβ = 0.03916 / 3600 * D2R * (cλp - sλp);
    const lonOut = pmod(s - 0.09033 / 3600 * D2R, 2 * Math.PI);
    const latOut = Δβ - lat;
    return { lon: lonOut, lat: latOut, range };
}
function aberration(range) {
    return -20.4898 / 3600 * D2R / range;
}
function apparentVSOP87(jde) {
    const { lon, lat, range } = trueVSOP87(jde);
    const Δψ = nutation(jde)[0];
    const a = aberration(range);
    return { lon: lon + Δψ + a, lat, range };
}
// ============================================================
// Solstice / Equinox
// ============================================================
// Table 27.A & 27.B coefficients
const mc0 = [1721139.29189, 365242.13740, 0.06134, 0.00111, -71e-5];
const jc0 = [1721233.25401, 365241.72562, -0.05323, 0.00907, 0.00025];
const sc0 = [1721325.70455, 365242.49558, -0.11677, -297e-5, 0.00074];
const dc0 = [1721414.39987, 365242.88257, -769e-5, -933e-5, -6e-5];
const mc2 = [2451623.80984, 365242.37404, 0.05169, -411e-5, -57e-5];
const jc2 = [2451716.56767, 365241.62603, 0.00325, 0.00888, -3e-4];
const sc2 = [2451810.21715, 365242.01767, -0.11575, 0.00337, 0.00078];
const dc2 = [2451900.05952, 365242.74049, -0.06223, -823e-5, 0.00032];
function eqHigh(year, planet, lon, c) {
    let J0 = horner(year * 0.001, ...c);
    for (;;) {
        const a = apparentVSOP87(J0);
        const correction = 58 * Math.sin(lon - a.lon);
        J0 += correction;
        if (Math.abs(correction) < 0.000005)
            break;
    }
    return J0;
}
function december2(year) {
    return longitude(year, Math.PI * 3 / 2);
}
function longitude(year, lon) {
    let c;
    let ct;
    let y = year;
    if (year < 1000) {
        ct = [mc0, jc0, sc0, dc0];
    }
    else {
        ct = [mc2, jc2, sc2, dc2];
        y = year - 2000;
    }
    lon = lon % (Math.PI * 2);
    if (lon < Math.PI / 2)
        c = ct[0];
    else if (lon < Math.PI)
        c = ct[1];
    else if (lon < Math.PI * 3 / 2)
        c = ct[2];
    else
        c = ct[3];
    return eqHigh(y, earth, lon, c);
}
// ============================================================
// Moon Phase
// ============================================================
const ck = 1 / 1236.85;
const meanLunarMonth = 29.530588861;
function moonMean(T) {
    return horner(T, 2451550.09766, 29.530588861 / ck, 0.00015437, -15e-8, 0.00000000073);
}
function moonSnap(y, q) {
    const k = (y - 2000) * 12.3685;
    return Math.floor(k - q + 0.5) + q;
}
// New moon coefficients
const nc = [
    -0.4072, 0.17241, 0.01608, 0.01039, 0.00739,
    -514e-5, 0.00208, -111e-5, -57e-5, 0.00056,
    -42e-5, 0.00042, 0.00038, -24e-5, -17e-5,
    -7e-5, 0.00004, 0.00004, 0.00003, 0.00003,
    -3e-5, 0.00003, -2e-5, -2e-5, 0.00002
];
// Additional corrections coefficients
const ac = [
    0.000325, 0.000165, 0.000164, 0.000126, 0.00011,
    0.000062, 0.00006, 0.000056, 0.000047, 0.000042,
    0.000040, 0.000037, 0.000035, 0.000023
];
function newMoon(year) {
    const k = moonSnap(year, 0);
    const T = k * ck;
    const E = horner(T, 1, -2516e-6, -74e-7);
    const M = horner(T, 2.5534 * D2R, 29.1053567 * D2R / ck, -14e-7 * D2R, -11e-8 * D2R);
    const M_ = horner(T, 201.5643 * D2R, 385.81693528 * D2R / ck, 0.0107582 * D2R, 0.00001238 * D2R, -58e-9 * D2R);
    const F = horner(T, 160.7108 * D2R, 390.67050284 * D2R / ck, -16118e-7 * D2R, -227e-8 * D2R, 0.000000011 * D2R);
    const Ω = horner(T, 124.7746 * D2R, -1.56375588 * D2R / ck, 0.0020672 * D2R, 0.00000215 * D2R);
    // Periodic terms for new/full moon
    const nfc = nc[0] * Math.sin(M_) +
        nc[1] * Math.sin(M) * E +
        nc[2] * Math.sin(2 * M_) +
        nc[3] * Math.sin(2 * F) +
        nc[4] * Math.sin(M_ - M) * E +
        nc[5] * Math.sin(M_ + M) * E +
        nc[6] * Math.sin(2 * M) * E * E +
        nc[7] * Math.sin(M_ - 2 * F) +
        nc[8] * Math.sin(M_ + 2 * F) +
        nc[9] * Math.sin(2 * M_ + M) * E +
        nc[10] * Math.sin(3 * M_) +
        nc[11] * Math.sin(M + 2 * F) * E +
        nc[12] * Math.sin(M - 2 * F) * E +
        nc[13] * Math.sin(2 * M_ - M) * E +
        nc[14] * Math.sin(Ω) +
        nc[15] * Math.sin(M_ + 2 * M) +
        nc[16] * Math.sin(2 * (M_ - F)) +
        nc[17] * Math.sin(3 * M) +
        nc[18] * Math.sin(M_ + M - 2 * F) +
        nc[19] * Math.sin(2 * (M_ + F)) +
        nc[20] * Math.sin(M_ + M + 2 * F) +
        nc[21] * Math.sin(M_ - M + 2 * F) +
        nc[22] * Math.sin(M_ - M - 2 * F) +
        nc[23] * Math.sin(3 * M_ + M) +
        nc[24] * Math.sin(4 * M_);
    // Additional corrections
    const A = new Array(14);
    A[0] = 299.7 * D2R + 0.107408 * D2R * k - 0.009173 * T * T;
    A[1] = 251.88 * D2R + 0.016321 * D2R * k;
    A[2] = 251.83 * D2R + 26.651886 * D2R * k;
    A[3] = 349.42 * D2R + 36.412478 * D2R * k;
    A[4] = 84.66 * D2R + 18.206239 * D2R * k;
    A[5] = 141.74 * D2R + 53.303771 * D2R * k;
    A[6] = 207.17 * D2R + 2.453732 * D2R * k;
    A[7] = 154.84 * D2R + 7.30686 * D2R * k;
    A[8] = 34.52 * D2R + 27.261239 * D2R * k;
    A[9] = 207.19 * D2R + 0.121824 * D2R * k;
    A[10] = 291.34 * D2R + 1.844379 * D2R * k;
    A[11] = 161.72 * D2R + 24.198154 * D2R * k;
    A[12] = 239.56 * D2R + 25.513099 * D2R * k;
    A[13] = 331.55 * D2R + 3.592518 * D2R * k;
    let addCorr = 0;
    for (let i = 0; i < 14; i++) {
        addCorr += ac[i] * Math.sin(A[i]);
    }
    return moonMean(T) + nfc + addCorr;
}
// ============================================================
// Exports (same interface as old astronomy.ts)
// ============================================================
// Module-level cache for expensive astronomical calculations
const cache = {
    lon: new Map(),
    sue: new Map(),
    ny: new Map()
};
function jdeToYear(jde) {
    return new CalendarGregorian().fromJDE(jde).toYear();
}
function toFixed(val, e) {
    return parseFloat(val.toFixed(e));
}
function apparentSolarLongitude(jde) {
    if (cache.lon.has(jde))
        return cache.lon.get(jde);
    const lon = apparentVSOP87(jde).lon;
    cache.lon.set(jde, lon);
    return lon;
}
function decemberSolstice(year) {
    if (cache.sue.has(year))
        return cache.sue.get(year);
    const jde = december2(year);
    cache.sue.set(year, jde);
    return jde;
}
function newMoonJDE(year) {
    return newMoon(year);
}
function solarTermLongitude(year, lonDeg) {
    return longitude(year, lonDeg * D2R);
}
function getCachedNewYear(year) {
    return cache.ny.get(year);
}
function setCachedNewYear(year, jde) {
    cache.ny.set(year, jde);
}
const MEAN_LUNAR_MONTH = meanLunarMonth;
const LUNAR_OFFSET = meanLunarMonth / 2;
const BESSELIAN_YEAR = BesselianYear;
const julian = { CalendarGregorian };
const DEG = 180 / Math.PI;

/**
 * Calendar configuration for each lunar calendar variant
 * Copyright (c) 2025 北京锋通科技有限公司 (郭玉峰, 吴琼). MIT License.
 *
 * Each variant differs in two key aspects:
 * 1. **Epoch year** — the starting point of the 60-year cycle count
 * 2. **Timezone offset** — determines which calendar day an astronomical event falls on
 *
 * The timezone offset is critical: the same new moon at 23:30 UTC+8 is "today" in China
 * but would be "yesterday" in UTC+9 (Japan). This can cause different leap month assignments.
 *
 * @module CalendarConfig
 *
 * @example
 * ```ts
 * import { CHINESE_CONFIG, JAPANESE_CONFIG, KOREAN_CONFIG } from 'chinese-date'
 *
 * CHINESE_CONFIG.epochYear   // -2636 (Chalmers epoch)
 * CHINESE_CONFIG.type        // "chinese"
 *
 * KOREAN_CONFIG.epochYear    // -2333 (Dangun epoch)
 * KOREAN_CONFIG.type         // "korean"
 * ```
 */
// Chinese Calendar epoch: 2636 BCE (Chalmers)
const CHINESE_EPOCH_YEAR = -2636;
const CHINESE_EPOCH_JDE = new julian.CalendarGregorian(CHINESE_EPOCH_YEAR, 2, 15).toJDE();
// Korean Calendar epoch: 2333 BCE (Dangun / 단군기원)
const KOREAN_EPOCH_YEAR = -2333;
const KOREAN_EPOCH_JDE = new julian.CalendarGregorian(KOREAN_EPOCH_YEAR, 1, 27).toJDE();
/**
 * Chinese calendar configuration (UTC+8, epoch 2636 BCE)
 *
 * Uses the Chalmers epoch starting from 2636 BCE.
 * Before 1929, Beijing local solar time (UTC+7:45:40) was used.
 * After 1929, standard China time (UTC+8) is used.
 */
const CHINESE_CONFIG = {
    type: 'chinese',
    epochYear: CHINESE_EPOCH_YEAR,
    epochJDE: CHINESE_EPOCH_JDE,
    timeshiftUTC(year, _month, _day) {
        if (year >= 1929)
            return 8 / 24; // UTC+8 Standard China time (120°E)
        return 1397 / 180 / 24; // UTC+7:45:40 Beijing (116°25'E)
    }
};
/**
 * Japanese calendar configuration (UTC+9, epoch 2636 BCE)
 *
 * Uses the same epoch as the Chinese calendar but with UTC+9 timezone.
 * The timezone difference can cause different leap month assignments
 * in rare cases where a new moon falls near midnight.
 *
 * Before 1888, Tokyo local solar time (UTC+9:19:04) was used.
 */
const JAPANESE_CONFIG = {
    type: 'japanese',
    epochYear: CHINESE_EPOCH_YEAR,
    epochJDE: CHINESE_EPOCH_JDE,
    timeshiftUTC(year, _month, _day) {
        if (year >= 1888)
            return 9 / 24; // UTC+9 (135°E) Japanese standard meridian
        return 4193 / 450 / 24; // UTC+9:19:04 (139°46'E)
    }
};
/**
 * Korean calendar configuration (UTC+8:30/9, epoch 2333 BCE — Dangun era)
 *
 * Uses the Dangun (단군기원) epoch starting from 2333 BCE.
 * This means Korean `cycle`/`year` values differ from Chinese for the same Gregorian date.
 *
 * Korea's timezone history is complex:
 * - Before 1908: Seoul local solar time (UTC+8:27:52)
 * - 1908-1911: UTC+8:30
 * - 1912-1954: UTC+9 (Japanese colonial period)
 * - 1954-1961: UTC+8:30
 * - 1961+: UTC+9
 */
const KOREAN_CONFIG = {
    type: 'korean',
    epochYear: KOREAN_EPOCH_YEAR,
    epochJDE: KOREAN_EPOCH_JDE,
    timeshiftUTC(year, month, day) {
        // Complex timezone history
        // @see https://en.wikipedia.org/wiki/Time_in_South_Korea
        const date = new Date(year, month - 1, day);
        const shifts = [
            { date: new Date(1961, 9, 10), shift: 9 / 24 }, // 1961-10-10 UTC+9
            { date: new Date(1954, 2, 21), shift: 8.5 / 24 }, // 1954-03-21 UTC+8:30
            { date: new Date(1911, 11, 31), shift: 9 / 24 }, // 1912-01-01 UTC+9
            { date: new Date(1908, 2, 31), shift: 8.5 / 24 } // 1908-04-01 UTC+8:30
        ];
        for (const s of shifts) {
            if (date >= s.date)
                return s.shift;
        }
        return 3809 / 450 / 24; // UTC+8:27:52 Seoul City Hall (126°58'E)
    }
};
/**
 * Vietnamese calendar configuration (UTC+7/8, epoch 2636 BCE)
 *
 * Uses the same epoch as the Chinese calendar but with different timezone:
 * - Before 1968: UTC+8 (same as China)
 * - After 1968: UTC+7
 *
 * The timezone difference can cause different leap month assignments
 * compared to the Chinese calendar, especially for recent dates.
 */
const VIETNAMESE_CONFIG = {
    type: 'vietnamese',
    epochYear: CHINESE_EPOCH_YEAR,
    epochJDE: CHINESE_EPOCH_JDE,
    timeshiftUTC(year, _month, _day) {
        if (year >= 1968)
            return 7 / 24; // UTC+7
        return 8 / 24; // UTC+8 Standard China time (120°E)
    }
};
/**
 * Get calendar configuration by type
 *
 * @param type - Calendar type: `'chinese'`, `'japanese'`, `'korean'`, or `'vietnamese'`
 * @returns The corresponding CalendarConfig object
 *
 * @example
 * ```ts
 * getConfig('chinese')   // CHINESE_CONFIG
 * getConfig('korean')    // KOREAN_CONFIG
 * ```
 */
function getConfig(type) {
    switch (type) {
        case 'chinese': return CHINESE_CONFIG;
        case 'japanese': return JAPANESE_CONFIG;
        case 'korean': return KOREAN_CONFIG;
        case 'vietnamese': return VIETNAMESE_CONFIG;
    }
}

/**
 * Core lunar calendar algorithm (pure functions)
 * Copyright (c) 2025 北京锋通科技有限公司 (郭玉峰, 吴琼). MIT License.
 *
 * This module provides low-level pure functions for lunar calendar computation.
 * Most users should prefer the high-level {@link ChineseDate} value object API.
 *
 * **When to use these functions directly:**
 * - You need fine-grained control over the calendar computation
 * - You want to build custom logic on top of the core algorithms
 * - You need to work with multiple calendar variants in the same code
 *
 * **Key concepts:**
 * - All functions take a {@link CalendarConfig} parameter to specify the calendar variant
 * - JDE (Julian Ephemeris Day) is used as the internal time representation
 * - "Midnight" is timezone-dependent, which is why timezone matters for calendar computation
 *
 * @module CoreAlgorithm
 */
// ─── Timezone & Midnight ──────────────────────────────────────────
/**
 * Truncate JDE to midnight in the calendar's local timezone
 *
 * Converts a JDE timestamp to the start of the local calendar day.
 * This is critical because the same astronomical event (e.g., new moon)
 * may fall on different calendar days depending on the timezone.
 *
 * @param config - Calendar configuration (provides timezone offset)
 * @param jde - Julian Ephemeris Day
 * @returns JDE truncated to local midnight
 */
function midnight(config, jde) {
    const gcal = new julian.CalendarGregorian().fromJDE(jde);
    const ts = 0.5 - config.timeshiftUTC(gcal.year, gcal.month, gcal.day);
    let mn = Math.trunc(gcal.toJD() - ts) + ts;
    mn = gcal.fromJD(mn).toJDE();
    if (toFixed(jde, 5) === toFixed(mn, 5) + 1) {
        return jde;
    }
    return mn;
}
// ─── Solar Terms ──────────────────────────────────────────────────
/**
 * Get major solar term (中气) for a given JDE
 *
 * A major solar term (中气) occurs when the apparent solar longitude
 * is a multiple of 30° starting from 330° (Z1=雨水, Z2=春分, ...).
 *
 * @param jde - Julian Ephemeris Day
 * @returns Object with term number (1-12) and solar longitude in degrees
 *
 * @example
 * ```ts
 * // Find which major solar term contains a given date
 * const result = inMajorSolarTerm(jde)
 * result.term  // 1-12 (Z1=雨水, Z2=春分, ..., Z12=大寒)
 * result.lon   // solar longitude in degrees
 * ```
 */
function inMajorSolarTerm(jde) {
    const lon = apparentSolarLongitude(jde);
    const lonDeg = lon * DEG - 1e-13;
    const term = (2 + Math.floor(lonDeg / 30)) % 12 + 1;
    return { term, lon: lonDeg };
}
/**
 * Test if a date falls inside a leap month
 *
 * A month is a leap month (闰月) if it does not contain a major solar term (中气).
 * This happens when two new moons fall within the same major solar term period.
 *
 * @param config - Calendar configuration
 * @param jde - Julian Ephemeris Day (any day within the month)
 * @returns `true` if the month containing this date is a leap month
 */
function isLeapMonth(config, jde) {
    const t1 = inMajorSolarTerm(jde);
    const next = nextNewMoon(config, midnight(config, jde + LUNAR_OFFSET));
    const t2 = inMajorSolarTerm(next);
    return t1.term === t2.term;
}
// ─── New Moon ─────────────────────────────────────────────────────
/**
 * Find the next new moon after a given JDE
 *
 * @param config - Calendar configuration (for timezone adjustment)
 * @param jde - Reference JDE (search starts after this)
 * @returns JDE of the next new moon, adjusted to local midnight
 */
function nextNewMoon(config, jde) {
    let nm = midnight(config, newMoonJDE(jdeToYear(jde)));
    let cnt = 0;
    while (nm < jde && cnt++ < 4) {
        nm = midnight(config, newMoonJDE(jdeToYear(jde + cnt * LUNAR_OFFSET)));
    }
    return nm;
}
/**
 * Find the previous new moon before a given JDE
 *
 * @param config - Calendar configuration (for timezone adjustment)
 * @param jde - Reference JDE (search starts before this)
 * @returns JDE of the previous new moon, adjusted to local midnight
 */
function previousNewMoon(config, jde) {
    let nm = midnight(config, newMoonJDE(jdeToYear(jde)));
    let cnt = 0;
    while (nm > jde && cnt++ < 4) {
        nm = midnight(config, newMoonJDE(jdeToYear(jde - cnt * LUNAR_OFFSET)));
    }
    return nm;
}
// ─── Chinese New Year ─────────────────────────────────────────────
/**
 * Get Chinese New Year (春节) JDE for a Gregorian year
 *
 * Computes the JDE of the first day of the lunar year (正月初一).
 * The algorithm determines if the preceding year has a leap month
 * (闰岁) and adjusts the new year date accordingly.
 *
 * Results are cached for performance.
 *
 * @param config - Calendar configuration
 * @param gyear - Gregorian year
 * @returns JDE of Chinese New Year
 *
 * @example
 * ```ts
 * const jde = newYear(CHINESE_CONFIG, 2024)
 * // Convert to Gregorian date to get the calendar date
 * const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5)
 * // gc.year=2024, gc.month=2, gc.day=10
 * ```
 */
function newYear(config, gyear) {
    gyear = Math.trunc(gyear);
    const cached = getCachedNewYear(gyear);
    if (cached !== undefined)
        return cached;
    const sue1 = decemberSolstice(gyear - 1);
    const sue2 = decemberSolstice(gyear);
    const m11n = previousNewMoon(config, midnight(config, sue2 + 1));
    const m12 = nextNewMoon(config, midnight(config, sue1 + 1));
    const m13 = nextNewMoon(config, midnight(config, m12 + LUNAR_OFFSET));
    const leapSui = Math.round((m11n - m12) / MEAN_LUNAR_MONTH) === 12;
    let ny = m13;
    if (leapSui && (isLeapMonth(config, m12) || isLeapMonth(config, m13))) {
        ny = nextNewMoon(config, midnight(config, m13 + MEAN_LUNAR_MONTH / 2));
    }
    setCachedNewYear(gyear, ny);
    return ny;
}
// ─── Gregorian → Lunar ───────────────────────────────────────────
/**
 * Convert Gregorian date to lunar date
 *
 * This is the primary conversion function. It computes the JDE of the
 * given Gregorian date, then determines the corresponding lunar date
 * by finding the enclosing new moon and major solar term.
 *
 * For dates in January before the 20th, the Gregorian year is adjusted
 * backward because Chinese New Year never starts before January 20.
 *
 * @param config - Calendar configuration (determines epoch and timezone)
 * @param year - Gregorian year (e.g., 2024)
 * @param month - Month (1-12)
 * @param day - Day of month
 * @returns Lunar date value with cycle/year/month/leap/day
 *
 * @example
 * ```ts
 * gregorianToLunar(CHINESE_CONFIG, 2024, 6, 21)
 * // { cycle: 78, year: 34, month: 5, leap: false, day: 6 }
 *
 * // Using Japanese calendar (different timezone may yield different results)
 * gregorianToLunar(JAPANESE_CONFIG, 2024, 6, 21)
 * // { cycle: 78, year: 34, month: 5, leap: false, day: 6 }
 * ```
 */
function gregorianToLunar(config, year, month, day) {
    const j = midnight(config, new julian.CalendarGregorian(year, month, day).toJDE());
    let gyear = year;
    if (month === 1 && day <= 20)
        gyear--; // Chinese New Year never starts before Jan 20
    return fromJDEWithYear(config, j, gyear);
}
/** Convert JDE to lunar date with known Gregorian year (internal) */
function fromJDEWithYear(config, j, gyear) {
    let ny = newYear(config, gyear);
    if (ny > j) {
        ny = newYear(config, gyear - 1);
    }
    let nm = previousNewMoon(config, j);
    if (nm < ny) {
        nm = ny;
    }
    // Calculate cycle and year from epoch
    const years = 1.5 + (ny - config.epochJDE) / BESSELIAN_YEAR;
    const cycle = 1 + Math.trunc((years - 1) / 60);
    const yearInCycle = 1 + Math.trunc((years - 1) % 60);
    // Determine month
    let lunarMonth = inMajorSolarTerm(nm).term;
    const m = Math.round((nm - ny) / MEAN_LUNAR_MONTH);
    let leap = false;
    if (m === 0) {
        lunarMonth = 1;
        leap = false;
    }
    else {
        leap = isLeapMonth(config, nm);
    }
    if (m > lunarMonth) {
        lunarMonth = m;
    }
    else if (leap) {
        lunarMonth--;
    }
    // Calculate day
    const lunarDay = 1 + Math.trunc(toFixed(j, 3) - toFixed(nm, 3));
    return { cycle, year: yearInCycle, month: lunarMonth, leap, day: lunarDay };
}
/**
 * Convert JDE to lunar date (public API)
 *
 * Given a JDE, determines the corresponding lunar date by finding
 * the enclosing new moon and computing the cycle/year/month/day.
 *
 * @param config - Calendar configuration
 * @param jde - Julian Ephemeris Day
 * @returns Lunar date value
 *
 * @example
 * ```ts
 * // JDE for 2024-06-21
 * fromJDE(CHINESE_CONFIG, 2460482.5)
 * // { cycle: 78, year: 34, month: 5, leap: false, day: 6 }
 * ```
 */
function fromJDE(config, jde) {
    const j = midnight(config, jde);
    const gc = new julian.CalendarGregorian().fromJDE(j);
    let gyear = gc.year;
    if (gc.month === 1 && gc.day < 20)
        gyear--;
    return fromJDEWithYear(config, j, gyear);
}
// ─── Lunar → Gregorian ───────────────────────────────────────────
/**
 * Get Gregorian year from lunar cycle/year
 *
 * Converts the cycle/year pair to a Gregorian year number using the
 * calendar's epoch year.
 *
 * @param config - Calendar configuration (provides epoch year)
 * @param cycle - 60-year cycle number (1-based)
 * @param year - Year within the cycle (1-60)
 * @returns Gregorian year number
 *
 * @example
 * ```ts
 * yearFromEpochCycle(CHINESE_CONFIG, 78, 34)  // 2024
 * yearFromEpochCycle(KOREAN_CONFIG, 78, 34)   // 2024 (same year, different epoch offset)
 * ```
 */
function yearFromEpochCycle(config, cycle, year) {
    return config.epochYear + (cycle - 1) * 60 + (year - 1);
}
/**
 * Convert lunar date to JDE
 *
 * Computes the JDE corresponding to a given lunar date by finding
 * the new moon that starts the month and adding the day offset.
 *
 * @param config - Calendar configuration
 * @param value - Lunar date value
 * @param gyear - Optional Gregorian year hint (optimizes computation)
 * @returns JDE of the lunar date
 *
 * @example
 * ```ts
 * lunarToJDE(CHINESE_CONFIG, { cycle: 78, year: 34, month: 5, leap: false, day: 6 })
 * // 2460481.5 (JDE for 2024-06-21)
 * ```
 */
function lunarToJDE(config, value, gyear) {
    const years = gyear || yearFromEpochCycle(config, value.cycle, value.year);
    const ny = newYear(config, years);
    let nm = ny;
    if (value.month > 1) {
        nm = previousNewMoon(config, ny + value.month * 29);
        const st = inMajorSolarTerm(nm).term;
        const lm = isLeapMonth(config, nm);
        if (st > value.month) {
            nm = previousNewMoon(config, nm - 1);
        }
        else if (st < value.month || (lm && !value.leap)) {
            nm = nextNewMoon(config, nm + 1);
        }
    }
    if (value.leap) {
        nm = nextNewMoon(config, nm + 1);
    }
    return nm + value.day - 1;
}
/**
 * Convert lunar date to Gregorian date
 *
 * The inverse of {@link gregorianToLunar}. Converts a lunar date value
 * to its corresponding Gregorian calendar date.
 *
 * @param config - Calendar configuration
 * @param value - Lunar date value
 * @param gyear - Optional Gregorian year hint (optimizes computation)
 * @returns Gregorian date object
 *
 * @example
 * ```ts
 * lunarToGregorian(CHINESE_CONFIG, { cycle: 78, year: 34, month: 5, leap: false, day: 6 })
 * // { year: 2024, month: 6, day: 21 }
 * ```
 */
function lunarToGregorian(config, value, gyear) {
    const jde = lunarToJDE(config, value, gyear);
    const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
    return {
        year: gc.year,
        month: gc.month,
        day: Math.trunc(gc.day)
    };
}
// ─── Solar Term JDE ──────────────────────────────────────────────
/**
 * Get JDE of a specific solar term (1-24)
 *
 * Solar terms are numbered 1-24:
 * 1=立春, 2=雨水, 3=惊蛰, 4=春分, ..., 23=小寒, 24=大寒.
 * Odd numbers are minor terms (节气), even numbers are major terms (中气).
 *
 * For terms 1-3 (立春, 雨水, 惊蛰), the Gregorian year is adjusted
 * backward by 1 because these terms belong to the previous solar year.
 *
 * @param config - Calendar configuration
 * @param term - Solar term number (1-24)
 * @param gyear - Gregorian year
 * @returns JDE of the solar term, adjusted to local midnight
 *
 * @example
 * ```ts
 * // 立春 (term 1) in 2024
 * solarTermJDE(CHINESE_CONFIG, 1, 2024)  // JDE for Feb 4, 2024
 * // 清明 (term 5) in 2024
 * solarTermJDE(CHINESE_CONFIG, 5, 2024)  // JDE for Apr 4, 2024
 * ```
 */
function solarTermJDE(config, term, gyear) {
    if (gyear && term <= 3)
        gyear--;
    const lon = (((term + 20) % 24) * 15) % 360;
    let st = solarTermLongitude(gyear, lon);
    st = midnight(config, st);
    return st;
}
/**
 * Get JDE of a major solar term (中气, Z1-Z12)
 *
 * Major solar terms occur when the sun's longitude is a multiple of 30°.
 * Z1=雨水, Z2=春分, Z3=谷雨, ..., Z12=大寒.
 *
 * @param config - Calendar configuration
 * @param term - Major term number (1-12)
 * @param gyear - Gregorian year
 * @returns JDE of the major solar term
 */
function majorSolarTermJDE(config, term, gyear) {
    return solarTermJDE(config, term * 2, gyear);
}
/**
 * Get JDE of a minor solar term (节气, J1-J12)
 *
 * Minor solar terms occur when the sun's longitude is at 15° + n*30°.
 * J1=立春, J2=惊蛰, J3=清明, ..., J12=小寒.
 *
 * @param config - Calendar configuration
 * @param term - Minor term number (1-12)
 * @param gyear - Gregorian year
 * @returns JDE of the minor solar term
 */
function minorSolarTermJDE(config, term, gyear) {
    return solarTermJDE(config, term * 2 - 1, gyear);
}

/** 天干 (Heavenly Stems) — the 10 stems in Chinese characters */
const GAN$1 = ['甲', '乙', '丙', '丁', '戊', '己', '庚', '辛', '壬', '癸'];
/** 地支 (Earthly Branches) — the 12 branches in Chinese characters */
const ZHI$1 = ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'];
/**
 * Get year GanZhi from the cycle position
 *
 * In the 60-year cycle, year=1 is 甲子, year=2 is 乙丑, etc.
 * The calculation uses the year position within the cycle to derive
 * both the stem (gan) and branch (zhi) indices.
 *
 * @param value - Lunar date value
 * @returns GanZhi pair for the year
 *
 * @example
 * ```ts
 * yearGanZhi({ cycle: 78, year: 1, month: 1, leap: false, day: 1 })
 * // { gan: 0, zhi: 0 } → 甲子
 * ```
 */
function yearGanZhi(value) {
    const idx = value.year - 1;
    return {
        gan: idx % 10,
        zhi: idx % 12
    };
}
/**
 * Get month GanZhi
 *
 * Month Zhi is fixed: 正月=寅, 二月=卯, ..., 十二月=丑.
 * Month Gan depends on the year's Gan using the 五虎遁月法 (Five Tigers method):
 * - 甲/己年 → 丙寅起 (start from 丙寅)
 * - 乙/庚年 → 戊寅起 (start from 戊寅)
 * - 丙/辛年 → 庚寅起 (start from 庚寅)
 * - 丁/壬年 → 壬寅起 (start from 壬寅)
 * - 戊/癸年 → 甲寅起 (start from 甲寅)
 *
 * @param value - Lunar date value
 * @returns GanZhi pair for the month
 *
 * @example
 * ```ts
 * // 甲辰年正月 → 丙寅
 * monthGanZhi({ cycle: 78, year: 1, month: 1, leap: false, day: 1 })
 * // { gan: 2, zhi: 2 }
 * ```
 */
function monthGanZhi(value) {
    // Month Zhi: 正月(1)=寅(2), 二月(2)=卯(3), ..., 十二月(12)=丑(1)
    const zhiIndex = (value.month + 1) % 12;
    // Month Gan base depends on year Gan (五虎遁月)
    const yearGanIdx = (value.year - 1) % 10;
    const baseGanMap = [2, 4, 6, 8, 0, 2, 4, 6, 8, 0];
    const baseGan = baseGanMap[yearGanIdx];
    // Month Gan increments from the base
    const ganIndex = (baseGan + value.month - 1) % 10;
    return { gan: ganIndex, zhi: zhiIndex };
}
/**
 * Get day GanZhi from JDE (Julian Ephemeris Day)
 *
 * Uses the continuous sexagenary cycle based on Julian Day Number.
 * The reference point: JDN 2451551 (2000-01-07) is 甲子日.
 *
 * @param jde - Julian Ephemeris Day
 * @returns GanZhi pair for the day
 *
 * @example
 * ```ts
 * dayGanZhi(2451551.5)  // { gan: 0, zhi: 0 } → 甲子
 * ```
 */
function dayGanZhi(jde) {
    const jdn = Math.floor(jde - 0.5);
    return {
        gan: ((jdn + 9) % 10 + 10) % 10,
        zhi: ((jdn + 1) % 12 + 12) % 12
    };
}
/**
 * Get full GanZhi info for a date (year, month, day pillars)
 *
 * This provides the three pillars of the 四柱八字 (Four Pillars of Destiny),
 * minus the hour pillar which requires time-of-day information.
 *
 * @param value - Lunar date value
 * @param jde - Julian Ephemeris Day (needed for day pillar calculation)
 * @returns Complete GanZhi info with year, month, and day pairs
 *
 * @example
 * ```ts
 * const info = getGanZhiInfo(value, jde)
 * // { year: { gan: 0, zhi: 0 }, month: { gan: 2, zhi: 2 }, day: { gan: 0, zhi: 0 } }
 * ```
 */
function getGanZhiInfo(value, jde) {
    return {
        year: yearGanZhi(value),
        month: monthGanZhi(value),
        day: dayGanZhi(jde)
    };
}
// ─── String helpers ───────────────────────────────────────────────
/**
 * Get a Heavenly Stem (天干) character by index
 *
 * @param index - Stem index (0-9), wraps around for out-of-range values
 * @returns The corresponding Chinese character
 *
 * @example
 * ```ts
 * ganChar(0)  // "甲"
 * ganChar(9)  // "癸"
 * ```
 */
function ganChar(index) {
    return GAN$1[((index % 10) + 10) % 10];
}
/**
 * Get an Earthly Branch (地支) character by index
 *
 * @param index - Branch index (0-11), wraps around for out-of-range values
 * @returns The corresponding Chinese character
 *
 * @example
 * ```ts
 * zhiChar(0)   // "子"
 * zhiChar(11)  // "亥"
 * ```
 */
function zhiChar(index) {
    return ZHI$1[((index % 12) + 12) % 12];
}
/**
 * Format a GanZhi pair as a string
 *
 * @param pair - The GanZhi pair to format
 * @returns The combined stem-branch string
 *
 * @example
 * ```ts
 * formatGanZhi({ gan: 0, zhi: 0 })  // "甲子"
 * formatGanZhi({ gan: 3, zhi: 9 })  // "丁酉"
 * ```
 */
function formatGanZhi(pair) {
    return ganChar(pair.gan) + zhiChar(pair.zhi);
}
/**
 * Get year GanZhi as a formatted string
 *
 * @param value - Lunar date value
 * @returns Year GanZhi string, e.g., "甲辰"
 */
function yearGanZhiStr(value) {
    return formatGanZhi(yearGanZhi(value));
}
/**
 * Get month GanZhi as a formatted string
 *
 * @param value - Lunar date value
 * @returns Month GanZhi string, e.g., "丙寅"
 */
function monthGanZhiStr(value) {
    return formatGanZhi(monthGanZhi(value));
}
/**
 * Get day GanZhi as a formatted string
 *
 * @param jde - Julian Ephemeris Day
 * @returns Day GanZhi string, e.g., "甲子"
 */
function dayGanZhiStr(jde) {
    return formatGanZhi(dayGanZhi(jde));
}
/**
 * Parse a GanZhi string back to pair indices
 *
 * @param str - A 2-character GanZhi string (e.g., "甲子")
 * @returns The corresponding GanZhi pair, or `null` if invalid
 *
 * @example
 * ```ts
 * parseGanZhi('甲子')  // { gan: 0, zhi: 0 }
 * parseGanZhi('丁酉')  // { gan: 3, zhi: 9 }
 * parseGanZhi('XX')    // null
 * ```
 */
function parseGanZhi(str) {
    if (str.length !== 2)
        return null;
    const gan = GAN$1.indexOf(str[0]);
    const zhi = ZHI$1.indexOf(str[1]);
    if (gan === -1 || zhi === -1)
        return null;
    return { gan, zhi };
}
/**
 * Get the 60-cycle index (1-60) from a GanZhi pair
 *
 * Uses the Chinese Remainder Theorem to find the unique position
 * in the 60-term sexagenary cycle.
 *
 * @param pair - The GanZhi pair
 * @returns Position in the cycle (1-60), where 1 = 甲子
 *
 * @example
 * ```ts
 * ganZhiToCycleIndex({ gan: 0, zhi: 0 })  // 1 (甲子)
 * ganZhiToCycleIndex({ gan: 9, zhi: 11 })  // 60 (癸亥)
 * ```
 */
function ganZhiToCycleIndex(pair) {
    for (let i = 0; i < 60; i++) {
        if (i % 10 === pair.gan && i % 12 === pair.zhi) {
            return i + 1;
        }
    }
    return 1; // fallback
}
/**
 * Get all 60 GanZhi combinations in order
 *
 * Returns the complete sexagenary cycle from 甲子 (1) to 癸亥 (60).
 *
 * @returns Array of 60 GanZhi strings
 *
 * @example
 * ```ts
 * allGanZhi()  // ["甲子", "乙丑", "丙寅", ..., "癸亥"]
 * ```
 */
function allGanZhi() {
    const result = [];
    for (let i = 0; i < 60; i++) {
        result.push(GAN$1[i % 10] + ZHI$1[i % 12]);
    }
    return result;
}

/** 生肖 — 简体中文 (Simplified Chinese) */
const ZODIAC_ZH_CN = ['鼠', '牛', '虎', '兔', '龙', '蛇', '马', '羊', '猴', '鸡', '狗', '猪'];
/** 生肖 — 繁體中文 (Traditional Chinese — Taiwan / Hong Kong) */
const ZODIAC_ZH_TW = ['鼠', '牛', '虎', '兔', '龍', '蛇', '馬', '羊', '猴', '雞', '狗', '豬'];
/** 生肖 — 日本語 (Japanese — uses branch characters) */
const ZODIAC_JA = ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'];
/** 生肖 — 한국어 (Korean — Hangul) */
const ZODIAC_KO = ['자', '축', '인', '묘', '진', '사', '오', '미', '신', '유', '술', '해'];
/** 生肖 — Tiếng Việt (Vietnamese) */
const ZODIAC_VI = ['Tý', 'Sửu', 'Dần', 'Mão', 'Thìn', 'Tỵ', 'Ngọ', 'Mùi', 'Thân', 'Dậu', 'Tuất', 'Hợi'];
const ZODIAC_MAP = {
    'zh-CN': ZODIAC_ZH_CN,
    'zh-TW': ZODIAC_ZH_TW,
    'zh-HK': ZODIAC_ZH_TW,
    'ja-JP': ZODIAC_JA,
    'ko-KR': ZODIAC_KO,
    'vi-VN': ZODIAC_VI
};
/**
 * Get zodiac animal index (0-11) from a lunar date
 *
 * The zodiac follows the earthly branch (地支) of the year.
 * Index 0 = 鼠 (Rat), index 11 = 猪 (Pig).
 *
 * @param value - Lunar date value
 * @returns Zodiac index (0-11)
 */
function zodiacIndex(value) {
    return (value.year - 1) % 12;
}
/**
 * Get zodiac animal name from a lunar date
 *
 * Returns the localized zodiac animal name. Key differences between locales:
 * - zh-CN uses simplified characters: 龙, 马, 鸡, 猪
 * - zh-TW/zh-HK use traditional characters: 龍, 馬, 雞, 豬
 * - ja-JP uses the branch character itself: 子, 丑, 寅...
 * - ko-KR uses Hangul: 자, 축, 인...
 * - vi-VN uses Vietnamese: Tý, Sửu, Dần...
 *
 * @param value - Lunar date value
 * @param locale - Locale code (default: `'zh-CN'`)
 * @returns Localized zodiac animal name
 *
 * @example
 * ```ts
 * zodiac(value, 'zh-CN')  // "龙"
 * zodiac(value, 'zh-TW')  // "龍"
 * zodiac(value, 'zh-HK')  // "龍"
 * zodiac(value, 'ja-JP')  // "辰"
 * ```
 */
function zodiac(value, locale = 'zh-CN') {
    const animals = ZODIAC_MAP[locale] || ZODIAC_ZH_CN;
    return animals[zodiacIndex(value)];
}
/**
 * Get zodiac animal from a Gregorian year
 *
 * **Note:** This uses the Gregorian year directly, which may differ
 * from the lunar year for dates before Chinese New Year.
 * For precise results, use {@link zodiac} with a `LunarDateValue`.
 *
 * @param gregorianYear - Gregorian year (e.g., 2024)
 * @param locale - Locale code (default: `'zh-CN'`)
 * @returns Localized zodiac animal name
 *
 * @example
 * ```ts
 * zodiacFromGregorianYear(2024, 'zh-CN')  // "龙"
 * zodiacFromGregorianYear(2024, 'zh-TW')  // "龍"
 * ```
 */
function zodiacFromGregorianYear(gregorianYear, locale = 'zh-CN') {
    const idx = ((gregorianYear - 4) % 12 + 12) % 12;
    const animals = ZODIAC_MAP[locale] || ZODIAC_ZH_CN;
    return animals[idx];
}
/**
 * Get zodiac emoji for a lunar date
 *
 * @param value - Lunar date value
 * @returns Emoji representation of the zodiac animal
 *
 * @example
 * ```ts
 * zodiacEmoji(value)  // "🐉"
 * ```
 */
function zodiacEmoji(value) {
    const emojis = ['🐀', '🐂', '🐅', '🐇', '🐉', '🐍', '🐴', '🐏', '🐵', '🐔', '🐶', '🐗'];
    return emojis[zodiacIndex(value)];
}
/**
 * Get all 12 zodiac animals for a locale
 *
 * @param locale - Locale code (default: `'zh-CN'`)
 * @returns Array of 12 localized zodiac animal names
 *
 * @example
 * ```ts
 * zodiacList('zh-CN')  // ["鼠", "牛", "虎", "兔", "龙", "蛇", "马", "羊", "猴", "鸡", "狗", "猪"]
 * zodiacList('zh-TW')  // ["鼠", "牛", "虎", "兔", "龍", "蛇", "馬", "羊", "猴", "雞", "狗", "豬"]
 * ```
 */
function zodiacList(locale = 'zh-CN') {
    return [...(ZODIAC_MAP[locale] || ZODIAC_ZH_CN)];
}

/**
 * 节日 (Festivals) — Traditional Lunar Calendar Festivals
 * Copyright (c) 2025 北京锋通科技有限公司 (郭玉峰, 吴琼). MIT License.
 *
 * Provides functions for looking up traditional festivals by lunar date
 * or by festival key. Festivals are region-aware: for example,
 * Spring Festival is called "春节" in zh-CN, "春節" in zh-TW,
 * and "農曆新年" in zh-HK.
 *
 * @module Festivals
 *
 * @example
 * ```ts
 * import { ChineseDate, getFestivalDate, getFestivalsInYear } from 'chinese-date'
 *
 * // Get festivals on a specific date
 * const date = ChineseDate.fromGregorian(2024, 2, 10)
 * date.getFestivals('zh-CN')  // [{ name: "春节", key: "spring", ... }]
 * date.getFestivals('zh-HK')  // [{ name: "農曆新年", key: "spring", ... }]
 *
 * // Get a festival's Gregorian date
 * ChineseDate.festivalDate('mid-autumn', 2024)  // { year: 2024, month: 9, day: 17 }
 *
 * // Get all festivals in a year
 * ChineseDate.festivalsInYear(2024, 'zh-TW')
 * ```
 */
/**
 * Traditional festival definitions
 */
const FESTIVALS = [
    // ─── 正月 ──────────────────────────────────────
    {
        key: 'spring', month: 1, day: 1, leap: false,
        names: {
            'zh-CN': '春节', 'zh-TW': '春節', 'zh-HK': '農曆新年',
            'ja-JP': '正月', 'ko-KR': '설날', 'vi-VN': 'Tết Nguyên Đán'
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR', 'VN']
    },
    {
        key: 'lantern', month: 1, day: 15, leap: false,
        names: {
            'zh-CN': '元宵节', 'zh-TW': '元宵節', 'zh-HK': '元宵節',
            'ja-JP': '小正月', 'ko-KR': '대보름', 'vi-VN': 'Tết Nguyên Tiêu'
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR', 'VN']
    },
    // ─── 二月 ──────────────────────────────────────
    {
        key: 'dragon-head', month: 2, day: 2, leap: false,
        names: {
            'zh-CN': '龙抬头', 'zh-TW': '龍抬頭', 'zh-HK': '龍抬頭',
            'ja-JP': '', 'ko-KR': '', 'vi-VN': ''
        },
        regions: ['CN', 'TW', 'HK']
    },
    // ─── 三月 ──────────────────────────────────────
    {
        key: 'shangsi', month: 3, day: 3, leap: false,
        names: {
            'zh-CN': '上巳节', 'zh-TW': '上巳節', 'zh-HK': '上巳節',
            'ja-JP': '', 'ko-KR': '삼짇날', 'vi-VN': ''
        },
        regions: ['CN', 'TW', 'HK', 'KR']
    },
    // ─── 五月 ──────────────────────────────────────
    {
        key: 'dragon-boat', month: 5, day: 5, leap: false,
        names: {
            'zh-CN': '端午节', 'zh-TW': '端午節', 'zh-HK': '端午節',
            'ja-JP': '端午の節句', 'ko-KR': '단오', 'vi-VN': 'Tết Đoan Ngọ'
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR', 'VN']
    },
    // ─── 七月 ──────────────────────────────────────
    {
        key: 'qixi', month: 7, day: 7, leap: false,
        names: {
            'zh-CN': '七夕节', 'zh-TW': '七夕', 'zh-HK': '七夕',
            'ja-JP': '七夕', 'ko-KR': '칠석', 'vi-VN': 'Thất Tịch'
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR', 'VN']
    },
    {
        key: 'ghost', month: 7, day: 15, leap: false,
        names: {
            'zh-CN': '中元节', 'zh-TW': '中元節', 'zh-HK': '中元節',
            'ja-JP': 'お盆', 'ko-KR': '백중', 'vi-VN': 'Lễ Vu Lan'
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR', 'VN']
    },
    // ─── 八月 ──────────────────────────────────────
    {
        key: 'mid-autumn', month: 8, day: 15, leap: false,
        names: {
            'zh-CN': '中秋节', 'zh-TW': '中秋節', 'zh-HK': '中秋節',
            'ja-JP': '十五夜', 'ko-KR': '추석', 'vi-VN': 'Tết Trung Thu'
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR', 'VN']
    },
    // ─── 九月 ──────────────────────────────────────
    {
        key: 'double-ninth', month: 9, day: 9, leap: false,
        names: {
            'zh-CN': '重阳节', 'zh-TW': '重陽節', 'zh-HK': '重陽節',
            'ja-JP': '重陽の節句', 'ko-KR': '중양절', 'vi-VN': ''
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR']
    },
    // ─── 腊月 ──────────────────────────────────────
    {
        key: 'laba', month: 12, day: 8, leap: false,
        names: {
            'zh-CN': '腊八节', 'zh-TW': '臘八節', 'zh-HK': '臘八節',
            'ja-JP': '', 'ko-KR': '', 'vi-VN': ''
        },
        regions: ['CN', 'TW', 'HK']
    },
    {
        key: 'little-new-year-north', month: 12, day: 23, leap: false,
        names: {
            'zh-CN': '小年（北方）', 'zh-TW': '', 'zh-HK': '',
            'ja-JP': '', 'ko-KR': '', 'vi-VN': ''
        },
        regions: ['CN']
    },
    {
        key: 'little-new-year-south', month: 12, day: 24, leap: false,
        names: {
            'zh-CN': '小年（南方）', 'zh-TW': '小年', 'zh-HK': '小年',
            'ja-JP': '', 'ko-KR': '', 'vi-VN': ''
        },
        regions: ['CN', 'TW', 'HK']
    },
    {
        key: 'new-year-eve', month: 12, day: 30, leap: false,
        names: {
            'zh-CN': '除夕', 'zh-TW': '除夕', 'zh-HK': '除夕',
            'ja-JP': '大晦日', 'ko-KR': '제야', 'vi-VN': 'Đêm giao thừa'
        },
        regions: ['CN', 'TW', 'HK', 'JP', 'KR', 'VN']
    }
];
const REGION_MAP = {
    'zh-CN': 'CN', 'zh-TW': 'TW', 'zh-HK': 'HK',
    'ja-JP': 'JP', 'ko-KR': 'KR', 'vi-VN': 'VN'
};
/**
 * Get festivals that fall on a specific lunar date
 *
 * Returns all festivals matching the given lunar date, filtered by
 * the locale's region. For example, `zh-HK` will return "農曆新年"
 * instead of "春节", and will not return "小年（北方）" which is
 * only observed in mainland China.
 *
 * @param value - Lunar date value
 * @param locale - Locale code (default: `'zh-CN'`)
 * @returns Array of matching festival info objects
 *
 * @example
 * ```ts
 * // 正月初一
 * getFestivals({ cycle: 78, year: 34, month: 1, leap: false, day: 1 }, 'zh-CN')
 * // [{ name: "春节", key: "spring", month: 1, day: 1, leap: false }]
 *
 * getFestivals({ cycle: 78, year: 34, month: 1, leap: false, day: 1 }, 'zh-HK')
 * // [{ name: "農曆新年", key: "spring", month: 1, day: 1, leap: false }]
 * ```
 */
function getFestivals(value, locale = 'zh-CN') {
    const region = REGION_MAP[locale] || 'CN';
    return FESTIVALS
        .filter(f => f.month === value.month &&
        f.day === value.day &&
        f.leap === value.leap &&
        f.regions.includes(region) &&
        f.names[locale])
        .map(f => ({
        name: f.names[locale] || f.names['zh-CN'] || f.key,
        key: f.key,
        month: f.month,
        day: f.day,
        leap: f.leap
    }));
}
/**
 * Get the Gregorian date of a specific festival in a given year
 *
 * @param config - Calendar configuration
 * @param key - Festival key (e.g., `'spring'`, `'mid-autumn'`, `'new-year-eve'`)
 * @param gyear - Gregorian year
 * @returns Gregorian date of the festival, or `null` if the key is not found
 *
 * @example
 * ```ts
 * getFestivalDate(CHINESE_CONFIG, 'spring', 2024)       // { year: 2024, month: 2, day: 10 }
 * getFestivalDate(CHINESE_CONFIG, 'mid-autumn', 2024)   // { year: 2024, month: 9, day: 17 }
 * getFestivalDate(CHINESE_CONFIG, 'new-year-eve', 2024) // { year: 2025, month: 1, day: 28 }
 * ```
 */
function getFestivalDate(config, key, gyear) {
    const festival = FESTIVALS.find(f => f.key === key);
    if (!festival)
        return null;
    // For 除夕, we need to find the last day of month 12
    if (key === 'new-year-eve') {
        return getNewYearEve(config, gyear);
    }
    // Find the lunar year that corresponds to this Gregorian year
    const lunarValue = {
        cycle: 0,
        year: 0,
        month: festival.month,
        leap: festival.leap,
        day: festival.day
    };
    // Approximate the lunar cycle/year for this Gregorian year
    const approxCycle = Math.ceil((gyear - config.epochYear) / 60);
    for (let c = approxCycle - 1; c <= approxCycle + 1; c++) {
        for (let y = 1; y <= 60; y++) {
            const gy = yearFromEpochCycle(config, c, y);
            if (gy === gyear || gy === gyear - 1) {
                lunarValue.cycle = c;
                lunarValue.year = y;
                try {
                    const greg = lunarToGregorian(config, lunarValue, gy);
                    if (greg.year === gyear) {
                        return greg;
                    }
                }
                catch {
                    // skip invalid combinations
                }
            }
        }
    }
    return null;
}
/**
 * Get New Year's Eve (除夕) date
 * 除夕 is the day before Chinese New Year
 */
function getNewYearEve(config, gyear) {
    const nyJDE = newYear(config, gyear + 1);
    // The day before new year is 除夕
    const gc = new julian.CalendarGregorian().fromJDE(nyJDE - 1 + 0.5);
    return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
}
/**
 * Get all festivals in a Gregorian year
 *
 * Returns all festivals for the specified year, filtered by the locale's region
 * and sorted by Gregorian date. Festivals without a localized name for the
 * requested locale are excluded.
 *
 * @param config - Calendar configuration
 * @param gyear - Gregorian year
 * @param locale - Locale code (default: `'zh-CN'`)
 * @returns Array of festival objects with name, key, and Gregorian date
 *
 * @example
 * ```ts
 * getFestivalsInYear(CHINESE_CONFIG, 2024, 'zh-CN')
 * // [{ name: "腊八节", key: "laba", date: { year: 2024, month: 1, day: 18 } }, ...]
 *
 * getFestivalsInYear(CHINESE_CONFIG, 2024, 'zh-HK')
 * // [{ name: "臘八節", key: "laba", date: { year: 2024, month: 1, day: 18 } }, ...]
 * ```
 */
function getFestivalsInYear(config, gyear, locale = 'zh-CN') {
    const result = [];
    const region = REGION_MAP[locale] || 'CN';
    for (const f of FESTIVALS) {
        if (!f.regions.includes(region))
            continue;
        if (!f.names[locale])
            continue;
        const date = getFestivalDate(config, f.key, gyear);
        if (date) {
            result.push({
                name: f.names[locale],
                key: f.key,
                date
            });
        }
    }
    result.sort((a, b) => {
        if (a.date.year !== b.date.year)
            return a.date.year - b.date.year;
        if (a.date.month !== b.date.month)
            return a.date.month - b.date.month;
        return a.date.day - b.date.day;
    });
    return result;
}
/**
 * Get all available festival keys
 *
 * @returns Array of festival key strings
 *
 * @example
 * ```ts
 * festivalKeys()
 * // ["spring", "lantern", "dragon-head", "shangsi", "dragon-boat",
 * //  "qixi", "ghost", "mid-autumn", "double-ninth", "laba",
 * //  "little-new-year-north", "little-new-year-south", "new-year-eve"]
 * ```
 */
function festivalKeys() {
    return FESTIVALS.map(f => f.key);
}

/**
 * 24 Solar Term names by locale
 *
 * Index 0 = term 1 (立春), index 23 = term 24 (大寒)
 *
 * Key Traditional Chinese differences:
 * - zh-TW/zh-HK: 驚蟄 (vs 惊蛰), 穀雨 (vs 谷雨), 處暑 (vs 处暑),
 *   小滿 (vs 小满), 芒種 (vs 芒种)
 * - ja-JP: 啓蟄 (vs 惊蛰), 処暑 (vs 处暑), 小満 (vs 小满)
 */
const SOLAR_TERM_NAMES = {
    'zh-CN': [
        '立春', '雨水', '惊蛰', '春分', '清明', '谷雨',
        '立夏', '小满', '芒种', '夏至', '小暑', '大暑',
        '立秋', '处暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'zh-TW': [
        '立春', '雨水', '驚蟄', '春分', '清明', '穀雨',
        '立夏', '小滿', '芒種', '夏至', '小暑', '大暑',
        '立秋', '處暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'zh-HK': [
        '立春', '雨水', '驚蟄', '春分', '清明', '穀雨',
        '立夏', '小滿', '芒種', '夏至', '小暑', '大暑',
        '立秋', '處暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'ja-JP': [
        '立春', '雨水', '啓蟄', '春分', '清明', '穀雨',
        '立夏', '小満', '芒種', '夏至', '小暑', '大暑',
        '立秋', '処暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'ko-KR': [
        '입춘', '우수', '경칩', '춘분', '청명', '곡우',
        '입하', '소만', '망종', '하지', '소서', '대서',
        '입추', '처서', '백로', '추분', '한로', '상강',
        '입동', '소설', '대설', '동지', '소한', '대한'
    ],
    'vi-VN': [
        'Lập xuân', 'Vũ thủy', 'Kinh trập', 'Xuân phân', 'Thanh minh', 'Cốc vũ',
        'Lập hạ', 'Tiểu mãn', 'Mang chủng', 'Hạ chí', 'Tiểu thử', 'Đại thử',
        'Lập thu', 'Xử thử', 'Bạch lộ', 'Thu phân', 'Hàn lộ', 'Sương giáng',
        'Lập đông', 'Tiểu tuyết', 'Đại tuyết', 'Đông chí', 'Tiểu hàn', 'Đại hàn'
    ]
};
/**
 * Get the JDE of a specific solar term
 *
 * @param config - Calendar configuration
 * @param term - Solar term number (1-24). 1=立春, 24=大寒
 * @param gyear - Gregorian year
 * @returns JDE (Julian Ephemeris Day) of the solar term
 */
function getSolarTermJDE(config, term, gyear) {
    return solarTermJDE(config, term, gyear);
}
/**
 * Get a specific solar term as a Gregorian date
 *
 * @param config - Calendar configuration
 * @param term - Solar term number (1-24). 1=立春, 2=雨水, ..., 24=大寒
 * @param gyear - Gregorian year
 * @returns Gregorian date of the solar term
 *
 * @example
 * ```ts
 * getSolarTerm(CHINESE_CONFIG, 5, 2024)
 * // { year: 2024, month: 4, day: 4 } — 清明
 * ```
 */
function getSolarTerm(config, term, gyear) {
    const jde = solarTermJDE(config, term, gyear);
    const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
    return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
}
/**
 * Get a specific major solar term (中气)
 *
 * Major solar terms occur when the sun's longitude is a multiple of 30°
 * starting from 0° (春分). They are used to determine leap months.
 *
 * @param config - Calendar configuration
 * @param term - Major term number (1-12). 1=雨水, 2=春分, ..., 12=大寒
 * @param gyear - Gregorian year
 * @returns Gregorian date of the major solar term
 */
function getMajorSolarTerm(config, term, gyear) {
    const jde = majorSolarTermJDE(config, term, gyear);
    const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
    return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
}
/**
 * Get a specific minor solar term (节气)
 *
 * Minor solar terms occur when the sun's longitude is at 15° + n*30°.
 *
 * @param config - Calendar configuration
 * @param term - Minor term number (1-12). 1=立春, 2=惊蛰, ..., 12=小寒
 * @param gyear - Gregorian year
 * @returns Gregorian date of the minor solar term
 */
function getMinorSolarTerm(config, term, gyear) {
    const jde = minorSolarTermJDE(config, term, gyear);
    const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
    return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
}
/**
 * Get Qingming (清明) date for a Gregorian year
 *
 * Qingming is the 5th solar term, typically falling on April 4 or 5.
 * It is culturally significant as the date for 扫墓 (tomb-sweeping).
 *
 * @param config - Calendar configuration
 * @param gyear - Gregorian year
 * @returns Gregorian date of Qingming
 *
 * @example
 * ```ts
 * getQingming(CHINESE_CONFIG, 2024)  // { year: 2024, month: 4, day: 4 }
 * ```
 */
function getQingming(config, gyear) {
    return getSolarTerm(config, 5, gyear);
}
/**
 * Get all 24 solar terms in a Gregorian year
 *
 * Returns an array of all 24 solar terms with their Gregorian dates and JDE values.
 * Note that some terms from adjacent years may be included (e.g., 小寒 and 大寒
 * of the previous year's winter solstice cycle).
 *
 * @param config - Calendar configuration
 * @param gyear - Gregorian year
 * @param locale - Locale for naming (default: `'zh-CN'`)
 * @returns Array of 24 solar term info objects
 *
 * @example
 * ```ts
 * const terms = getSolarTermsInYear(CHINESE_CONFIG, 2024, 'zh-CN')
 * terms.forEach(t => console.log(t.index, solarTermName(t.index, 'zh-TW'), t.date))
 * ```
 */
function getSolarTermsInYear(config, gyear, locale = 'zh-CN') {
    const result = [];
    for (let i = 1; i <= 24; i++) {
        const jde = solarTermJDE(config, i, gyear);
        const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
        result.push({
            index: i,
            date: { year: gc.year, month: gc.month, day: Math.trunc(gc.day) },
            jde
        });
    }
    return result;
}
/**
 * Get solar term name by index (1-24) and locale
 *
 * Key Traditional Chinese differences from Simplified:
 * - 惊蛰 → 驚蟄 (zh-TW/zh-HK), 啓蟄 (ja-JP)
 * - 谷雨 → 穀雨 (zh-TW/zh-HK/ja-JP)
 * - 处暑 → 處暑 (zh-TW/zh-HK), 処暑 (ja-JP)
 * - 小满 → 小滿 (zh-TW/zh-HK), 小満 (ja-JP)
 * - 芒种 → 芒種 (zh-TW/zh-HK/ja-JP)
 *
 * @param term - Solar term number (1-24)
 * @param locale - Locale code (default: `'zh-CN'`)
 * @returns Localized solar term name
 *
 * @example
 * ```ts
 * solarTermName(3, 'zh-CN')  // "惊蛰"
 * solarTermName(3, 'zh-TW')  // "驚蟄"
 * solarTermName(3, 'zh-HK')  // "驚蟄"
 * solarTermName(3, 'ja-JP')  // "啓蟄"
 * ```
 */
function solarTermName(term, locale = 'zh-CN') {
    const names = SOLAR_TERM_NAMES[locale] || SOLAR_TERM_NAMES['zh-CN'];
    return names[term - 1] || '';
}
/**
 * Get all 24 solar term names for a locale
 *
 * @param locale - Locale code (default: `'zh-CN'`)
 * @returns Array of 24 localized solar term names
 */
function solarTermNames(locale = 'zh-CN') {
    return [...(SOLAR_TERM_NAMES[locale] || SOLAR_TERM_NAMES['zh-CN'])];
}
/**
 * Find which solar term a Gregorian date falls on (if any)
 *
 * Checks all 24 solar terms for the given year and adjacent years
 * to determine if the specified date coincides with a solar term day.
 *
 * @param config - Calendar configuration
 * @param year - Gregorian year
 * @param month - Month (1-12)
 * @param day - Day of month
 * @param locale - Locale for naming (default: `'zh-CN'`)
 * @returns Solar term name if the date is a solar term day, otherwise `null`
 *
 * @example
 * ```ts
 * findSolarTermOnDate(CHINESE_CONFIG, 2024, 4, 4, 'zh-CN')   // "清明"
 * findSolarTermOnDate(CHINESE_CONFIG, 2024, 4, 4, 'zh-TW')   // "清明"
 * findSolarTermOnDate(CHINESE_CONFIG, 2024, 6, 5, 'zh-CN')   // "芒种"
 * findSolarTermOnDate(CHINESE_CONFIG, 2024, 6, 5, 'zh-HK')   // "芒種"
 * findSolarTermOnDate(CHINESE_CONFIG, 2024, 6, 15, 'zh-CN')  // null
 * ```
 */
function findSolarTermOnDate(config, year, month, day, locale = 'zh-CN') {
    for (const y of [year, year - 1, year + 1]) {
        const terms = getSolarTermsInYear(config, y, locale);
        for (const t of terms) {
            if (t.date.year === year && t.date.month === month && t.date.day === day) {
                return solarTermName(t.index, locale);
            }
        }
    }
    return null;
}

// ─── 天干 (Heavenly Stems) ────────────────────────────────────────
const GAN = {
    'zh-CN': ['甲', '乙', '丙', '丁', '戊', '己', '庚', '辛', '壬', '癸'],
    'zh-TW': ['甲', '乙', '丙', '丁', '戊', '己', '庚', '辛', '壬', '癸'],
    'zh-HK': ['甲', '乙', '丙', '丁', '戊', '己', '庚', '辛', '壬', '癸'],
    'ja-JP': ['甲', '乙', '丙', '丁', '戊', '己', '庚', '辛', '壬', '癸'],
    'ko-KR': ['갑', '을', '병', '정', '무', '기', '경', '신', '임', '계'],
    'vi-VN': ['Giáp', 'Ất', 'Bính', 'Đinh', 'Mậu', 'Kỷ', 'Canh', 'Tân', 'Nhâm', 'Quý']
};
// ─── 地支 (Earthly Branches) ──────────────────────────────────────
const ZHI = {
    'zh-CN': ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'],
    'zh-TW': ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'],
    'zh-HK': ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'],
    'ja-JP': ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'],
    'ko-KR': ['자', '축', '인', '묘', '진', '사', '오', '미', '신', '유', '술', '해'],
    'vi-VN': ['Tý', 'Sửu', 'Dần', 'Mão', 'Thìn', 'Tỵ', 'Ngọ', 'Mùi', 'Thân', 'Dậu', 'Tuất', 'Hợi']
};
// ─── 生肖 (Zodiac) ────────────────────────────────────────────────
const ZODIAC = {
    'zh-CN': ['鼠', '牛', '虎', '兔', '龙', '蛇', '马', '羊', '猴', '鸡', '狗', '猪'],
    'zh-TW': ['鼠', '牛', '虎', '兔', '龍', '蛇', '馬', '羊', '猴', '雞', '狗', '豬'],
    'zh-HK': ['鼠', '牛', '虎', '兔', '龍', '蛇', '馬', '羊', '猴', '雞', '狗', '豬'],
    'ja-JP': ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'],
    'ko-KR': ['자(鼠)', '축(牛)', '인(虎)', '묘(兔)', '진(龍)', '사(蛇)', '오(馬)', '미(羊)', '신(猴)', '유(鷄)', '술(狗)', '해(豬)'],
    'vi-VN': ['Tý', 'Sửu', 'Dần', 'Mão', 'Thìn', 'Tỵ', 'Ngọ', 'Mùi', 'Thân', 'Dậu', 'Tuất', 'Hợi']
};
// ─── 月份名称 (Month Names) ──────────────────────────────────────
const MONTH_NAMES_ZH = ['正月', '二月', '三月', '四月', '五月', '六月',
    '七月', '八月', '九月', '十月', '十一月', '腊月'];
const MONTH_NAMES_ZH_TW = ['正月', '二月', '三月', '四月', '五月', '六月',
    '七月', '八月', '九月', '十月', '十一月', '臘月'];
const MONTH_NAMES_JA = ['睦月', '如月', '弥生', '卯月', '皐月', '水無月',
    '文月', '葉月', '長月', '神無月', '霜月', '師走'];
const MONTH_NAMES_KO = ['정월', '이월', '삼월', '사월', '오월', '유월',
    '칠월', '팔월', '구월', '시월', '십일월', '섣달'];
const MONTH_NAMES_VI = ['Tháng Giêng', 'Tháng 2', 'Tháng 3', 'Tháng 4',
    'Tháng 5', 'Tháng 6', 'Tháng 7', 'Tháng 8', 'Tháng 9',
    'Tháng 10', 'Tháng 11', 'Tháng Chạp'];
const LEAP_PREFIX = {
    'zh-CN': '闰',
    'zh-TW': '閏',
    'zh-HK': '閏',
    'ja-JP': '閏',
    'ko-KR': '윤',
    'vi-VN': 'Nhuận '
};
const MONTH_NAMES = {
    'zh-CN': MONTH_NAMES_ZH,
    'zh-TW': MONTH_NAMES_ZH_TW,
    'zh-HK': MONTH_NAMES_ZH_TW,
    'ja-JP': MONTH_NAMES_JA,
    'ko-KR': MONTH_NAMES_KO,
    'vi-VN': MONTH_NAMES_VI
};
// ─── 日名称 (Day Names) ──────────────────────────────────────────
function chineseDayName(day) {
    const units = ['', '一', '二', '三', '四', '五', '六', '七', '八', '九', '十'];
    if (day === 10)
        return '初十';
    if (day === 20)
        return '二十';
    if (day === 30)
        return '三十';
    const t = Math.floor(day / 10);
    const u = day % 10;
    // day 1-9: 初一 ~ 初九
    if (t === 0)
        return '初' + units[u];
    // day 11-19: 十一 ~ 十九
    if (t === 1)
        return '十' + units[u];
    // day 21-29: 廿一 ~ 廿九
    if (t === 2)
        return '廿' + units[u];
    return String(day);
}
function koreanDayName(day) {
    return day + '일';
}
function vietnameseDayName(day) {
    return 'Ngày ' + day;
}
// ─── 24节气名称 (Solar Term Names) ──────────────────────────────
const SOLAR_TERMS = {
    'zh-CN': [
        '立春', '雨水', '惊蛰', '春分', '清明', '谷雨',
        '立夏', '小满', '芒种', '夏至', '小暑', '大暑',
        '立秋', '处暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'zh-TW': [
        '立春', '雨水', '驚蟄', '春分', '清明', '穀雨',
        '立夏', '小滿', '芒種', '夏至', '小暑', '大暑',
        '立秋', '處暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'zh-HK': [
        '立春', '雨水', '驚蟄', '春分', '清明', '穀雨',
        '立夏', '小滿', '芒種', '夏至', '小暑', '大暑',
        '立秋', '處暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'ja-JP': [
        '立春', '雨水', '啓蟄', '春分', '清明', '穀雨',
        '立夏', '小満', '芒種', '夏至', '小暑', '大暑',
        '立秋', '処暑', '白露', '秋分', '寒露', '霜降',
        '立冬', '小雪', '大雪', '冬至', '小寒', '大寒'
    ],
    'ko-KR': [
        '입춘', '우수', '경칩', '춘분', '청명', '곡우',
        '입하', '소만', '망종', '하지', '소서', '대서',
        '입추', '처서', '백로', '추분', '한로', '상강',
        '입동', '소설', '대설', '동지', '소한', '대한'
    ],
    'vi-VN': [
        'Lập xuân', 'Vũ thủy', 'Kinh trập', 'Xuân phân', 'Thanh minh', 'Cốc vũ',
        'Lập hạ', 'Tiểu mãn', 'Mang chủng', 'Hạ chí', 'Tiểu thử', 'Đại thử',
        'Lập thu', 'Xử thử', 'Bạch lộ', 'Thu phân', 'Hàn lộ', 'Sương giáng',
        'Lập đông', 'Tiểu tuyết', 'Đại tuyết', 'Đông chí', 'Tiểu hàn', 'Đại hàn'
    ]
};
// ─── 年份格式 (Year Format) ──────────────────────────────────────
const YEAR_FORMATS = {
    'zh-CN': (ganZhi, zodiac) => `${ganZhi}${zodiac}年`,
    'zh-TW': (ganZhi, zodiac) => `${ganZhi}${zodiac}年`,
    'zh-HK': (ganZhi, zodiac) => `${ganZhi}${zodiac}年`,
    'ja-JP': (ganZhi, _zodiac) => `${ganZhi}年`,
    'ko-KR': (ganZhi, _zodiac) => `${ganZhi}년`,
    'vi-VN': (ganZhi, _zodiac) => `Năm ${ganZhi}`
};
// ─── Locale Data Assembly ─────────────────────────────────────────
function buildLocaleData(code) {
    const dayNameFn = {
        'zh-CN': chineseDayName,
        'zh-TW': chineseDayName,
        'zh-HK': chineseDayName,
        'ja-JP': chineseDayName,
        'ko-KR': koreanDayName,
        'vi-VN': vietnameseDayName
    };
    return {
        code,
        gan: GAN[code],
        zhi: ZHI[code],
        zodiac: ZODIAC[code],
        monthNames: (month, leap) => {
            const names = MONTH_NAMES[code];
            const name = names[month - 1] || `${month}月`;
            return leap ? LEAP_PREFIX[code] + name : name;
        },
        dayNames: dayNameFn[code],
        solarTerms: SOLAR_TERMS[code],
        festivals: {}, // Festival names are managed in festival.ts
        yearFormat: YEAR_FORMATS[code]
    };
}
// ─── Locale Registry ──────────────────────────────────────────────
const locales = new Map();
/**
 * Get locale data for the specified locale code
 *
 * Returns a cached {@link LocaleData} object containing all localized
 * strings and formatting functions for the given locale.
 *
 * @param code - Locale code (e.g., `'zh-CN'`, `'zh-TW'`, `'ja-JP'`, `'ko-KR'`, `'vi-VN'`)
 * @returns Locale data object
 *
 * @example
 * ```ts
 * const loc = getLocale('zh-TW')
 * loc.zodiac            // ["鼠", "牛", "虎", "兔", "龍", "蛇", "馬", "羊", "猴", "雞", "狗", "豬"]
 * loc.monthNames(12, false)  // "臘月"
 * loc.monthNames(4, true)    // "閏四月"
 * loc.dayNames(15)           // "十五"
 * loc.gan                    // ["甲", "乙", "丙", ...]
 * loc.solarTerms             // ["立春", "雨水", "驚蟄", ...]
 * loc.yearFormat('甲辰', '龍')  // "甲辰龍年"
 * ```
 */
function getLocale(code) {
    if (!locales.has(code)) {
        locales.set(code, buildLocaleData(code));
    }
    return locales.get(code);
}
/**
 * Get all available locale codes
 *
 * @returns Array of supported locale codes: `['zh-CN', 'zh-TW', 'zh-HK', 'ja-JP', 'ko-KR', 'vi-VN']`
 *
 * @example
 * ```ts
 * availableLocales()  // ['zh-CN', 'zh-TW', 'zh-HK', 'ja-JP', 'ko-KR', 'vi-VN']
 * ```
 */
function availableLocales() {
    return ['zh-CN', 'zh-TW', 'zh-HK', 'ja-JP', 'ko-KR', 'vi-VN'];
}
/**
 * Check if a locale code is supported
 *
 * @param code - Locale code to check
 * @returns `true` if the locale is supported (type-narrows to `LocaleCode`)
 *
 * @example
 * ```ts
 * isLocaleSupported('zh-CN')   // true
 * isLocaleSupported('en-US')   // false
 * ```
 */
function isLocaleSupported(code) {
    return ['zh-CN', 'zh-TW', 'zh-HK', 'ja-JP', 'ko-KR', 'vi-VN'].includes(code);
}

/**
 * 中文数字 (Chinese Numerals) — 数字与中文数字的转换
 * Copyright (c) 2025 北京锋通科技有限公司 (郭玉峰, 吴琼). MIT License.
 *
 * 提供两种中文数字表示：
 * - 大写（金融/正式）：零壹贰叁肆伍陆柒捌玖
 * - 日期用（日常）：〇一二三四五六七八九
 * - 小写（口语）：零一二三四五六七八九
 *
 * @module ChineseNumerals
 *
 * @example
 * ```ts
 * import { toChineseYear, toChineseNumber } from './chinese-numerals'
 *
 * toChineseYear(2025, 'upper')   // "二〇二五"
 * toChineseYear(2025, 'lower')   // "二零二五"
 * toChineseNumber(11, 'upper')   // "十一"
 * toChineseNumber(21, 'upper')   // "二十一"
 * ```
 */
/** 日期用中文数字（〇版） */
const DIGITS_DATE = ['〇', '一', '二', '三', '四', '五', '六', '七', '八', '九'];
/** 小写中文数字（零版） */
const DIGITS_LOWER = ['零', '一', '二', '三', '四', '五', '六', '七', '八', '九'];
/**
 * 将年份数字逐位转换为中文数字
 *
 * @param year - 公历年份，如 2025
 * @param style - 'upper' 使用〇（日期常用），'lower' 使用零（口语常用）
 * @returns 中文数字年份，如 "二〇二五" 或 "二零二五"
 *
 * @example
 * toChineseYear(2025, 'upper') // "二〇二五"
 * toChineseYear(2025, 'lower') // "二零二五"
 * toChineseYear(1900, 'upper') // "一九〇〇"
 */
function toChineseYear(year, style = 'upper') {
    const digits = style === 'upper' ? DIGITS_DATE : DIGITS_LOWER;
    return String(year).split('').map(d => digits[Number(d)]).join('');
}
/**
 * 将 1-99 的数字转换为中文数字（用于月、日）
 *
 * @param num - 数字（1-99）
 * @param style - 'upper' 或 'lower'（仅影响"零"的写法）
 * @returns 中文数字，如 "十"、"二十一"、"三"
 *
 * @example
 * toChineseNumber(1, 'upper')   // "一"
 * toChineseNumber(10, 'upper')  // "十"
 * toChineseNumber(11, 'upper')  // "十一"
 * toChineseNumber(21, 'upper')  // "二十一"
 * toChineseNumber(30, 'upper')  // "三十"
 */
function toChineseNumber(num, style = 'upper') {
    if (num < 1 || num > 99)
        return String(num);
    const digits = style === 'upper' ? DIGITS_DATE : DIGITS_LOWER;
    if (num < 10)
        return digits[num];
    if (num === 10)
        return '十';
    if (num < 20)
        return '十' + digits[num % 10];
    if (num % 10 === 0)
        return digits[Math.floor(num / 10)] + '十';
    return digits[Math.floor(num / 10)] + '十' + digits[num % 10];
}
/**
 * 星期名称映射
 */
const WEEKDAY_NAMES = {
    'zh-CN': ['星期日', '星期一', '星期二', '星期三', '星期四', '星期五', '星期六'],
    'zh-TW': ['星期日', '星期一', '星期二', '星期三', '星期四', '星期五', '星期六'],
    'zh-HK': ['星期日', '星期一', '星期二', '星期三', '星期四', '星期五', '星期六'],
    'ja-JP': ['日曜日', '月曜日', '火曜日', '水曜日', '木曜日', '金曜日', '土曜日'],
    'ko-KR': ['일요일', '월요일', '화요일', '수요일', '목요일', '금요일', '토요일'],
    'vi-VN': ['Chủ nhật', 'Thứ Hai', 'Thứ Ba', 'Thứ Tư', 'Thứ Năm', 'Thứ Sáu', 'Thứ Bảy']
};
const WEEKDAY_SHORT = {
    'zh-CN': ['日', '一', '二', '三', '四', '五', '六'],
    'zh-TW': ['日', '一', '二', '三', '四', '五', '六'],
    'zh-HK': ['日', '一', '二', '三', '四', '五', '六'],
    'ja-JP': ['日', '月', '火', '水', '木', '金', '土'],
    'ko-KR': ['일', '월', '화', '수', '목', '금', '토'],
    'vi-VN': ['CN', 'T2', 'T3', 'T4', 'T5', 'T6', 'T7']
};
/**
 * 获取星期几名称
 *
 * @param weekday - 0-6 (0=周日, 1=周一, ..., 6=周六)
 * @param locale - 地区代码（默认 `'zh-CN'`）
 * @returns 星期几名称
 *
 * @example
 * ```ts
 * getWeekdayName(0, 'zh-CN')  // "星期日"
 * getWeekdayName(6, 'zh-CN')  // "星期六"
 * getWeekdayName(6, 'ja-JP')  // "土曜日"
 * getWeekdayName(6, 'ko-KR')  // "토요일"
 * getWeekdayName(6, 'vi-VN')  // "Thứ Bảy"
 * ```
 */
function getWeekdayName(weekday, locale = 'zh-CN') {
    return WEEKDAY_NAMES[locale]?.[weekday] ?? WEEKDAY_NAMES['zh-CN'][weekday];
}
/**
 * 获取星期几短名称
 *
 * @param weekday - 0-6 (0=周日, 1=周一, ..., 6=周六)
 * @param locale - 地区代码（默认 `'zh-CN'`）
 * @returns 星期几短名称
 *
 * @example
 * ```ts
 * getWeekdayShort(6, 'zh-CN')  // "六"
 * getWeekdayShort(6, 'ja-JP')  // "土"
 * getWeekdayShort(6, 'ko-KR')  // "토"
 * getWeekdayShort(6, 'vi-VN')  // "T7"
 * ```
 */
function getWeekdayShort(weekday, locale = 'zh-CN') {
    return WEEKDAY_SHORT[locale]?.[weekday] ?? WEEKDAY_SHORT['zh-CN'][weekday];
}

function format(value, jde, gregOrPattern, patternOrLocale, locale) {
    // Overload resolution: format(value, jde, pattern, locale?) or format(value, jde, greg, pattern, locale?)
    let greg;
    let pattern;
    let locCode;
    if (typeof gregOrPattern === 'string') {
        // format(value, jde, pattern, locale?)
        pattern = gregOrPattern;
        locCode = patternOrLocale ?? 'zh-CN';
    }
    else {
        // format(value, jde, greg, pattern, locale?)
        greg = gregOrPattern;
        pattern = patternOrLocale;
        locCode = locale ?? 'zh-CN';
    }
    const loc = getLocale(locCode);
    const yGz = yearGanZhi(value);
    const yGzStr = formatGanZhi(yGz);
    const zodiacStr = zodiac(value, locCode);
    let result = pattern
        // Lunar tokens
        .replace(/\{CY\}/g, String(value.cycle))
        .replace(/\{Y\}/g, String(value.year))
        .replace(/\{Ygz\}/g, yGzStr)
        .replace(/\{Z\}/g, zodiacStr)
        .replace(/\{M\}/g, String(value.month))
        .replace(/\{Mname\}/g, loc.monthNames(value.month, value.leap))
        .replace(/\{L\}/g, value.leap ? loc.code === 'zh-CN' ? '闰' : loc.code.startsWith('zh') ? '閏' : '' : '')
        .replace(/\{D\}/g, String(value.day))
        .replace(/\{Dname\}/g, loc.dayNames(value.day));
    // Gregorian tokens (require gregorian date)
    if (greg) {
        const weekday = getWeekdayFromGregorian(greg.year, greg.month, greg.day);
        result = result
            .replace(/\{GY\}/g, String(greg.year))
            .replace(/\{GYcn\}/g, toChineseYear(greg.year, 'upper'))
            .replace(/\{GYcn2\}/g, toChineseYear(greg.year, 'lower'))
            .replace(/\{GM\}/g, String(greg.month))
            .replace(/\{GM0\}/g, String(greg.month).padStart(2, '0'))
            .replace(/\{GMcn\}/g, toChineseNumber(greg.month))
            .replace(/\{GD\}/g, String(greg.day))
            .replace(/\{GD0\}/g, String(greg.day).padStart(2, '0'))
            .replace(/\{GDcn\}/g, toChineseNumber(greg.day))
            .replace(/\{W\}/g, String(weekday))
            .replace(/\{Wname\}/g, getWeekdayName(weekday, locCode))
            .replace(/\{Wshort\}/g, getWeekdayShort(weekday, locCode));
    }
    else {
        // Remove gregorian tokens if no gregorian date provided
        result = result
            .replace(/\{GY\}/g, '')
            .replace(/\{GYcn\}/g, '')
            .replace(/\{GYcn2\}/g, '')
            .replace(/\{GM\}/g, '')
            .replace(/\{GM0\}/g, '')
            .replace(/\{GMcn\}/g, '')
            .replace(/\{GD\}/g, '')
            .replace(/\{GD0\}/g, '')
            .replace(/\{GDcn\}/g, '')
            .replace(/\{W\}/g, '')
            .replace(/\{Wname\}/g, '')
            .replace(/\{Wshort\}/g, '');
    }
    return result;
}
/**
 * Compute weekday (0=Sun, 6=Sat) from Gregorian date
 * @internal
 */
function getWeekdayFromGregorian(year, month, day) {
    return new Date(year, month - 1, day).getDay();
}
/**
 * Default format: full Chinese date string
 * e.g., "甲辰龙年 五月廿一"
 */
function formatFull(value, jde, locale = 'zh-CN') {
    const loc = getLocale(locale);
    const yGzStr = formatGanZhi(yearGanZhi(value));
    const zodiacStr = zodiac(value, locale);
    const yearPart = loc.yearFormat(yGzStr, zodiacStr);
    const monthPart = loc.monthNames(value.month, value.leap);
    const dayPart = loc.dayNames(value.day);
    return `${yearPart} ${monthPart}${dayPart}`;
}
/**
 * Compact format: cycle/year/month/leap/day
 * e.g., "78/34/7/0/28"
 */
function formatCompact(value) {
    return `${value.cycle}/${value.year}/${value.month}/${value.leap ? 1 : 0}/${value.day}`;
}
/**
 * ISO-like format: CY-Y-M-L-D
 * e.g., "78-34-7-0-28"
 */
function formatISO(value) {
    return `${value.cycle}-${value.year}-${value.month}-${value.leap ? 1 : 0}-${value.day}`;
}
/**
 * GanZhi format: year month day GanZhi
 * e.g., "甲辰年 丙寅月 甲子日"
 */
function formatGanZhiFull(value, jde, locale = 'zh-CN') {
    const yGz = formatGanZhi(yearGanZhi(value));
    const mGz = formatGanZhi(monthGanZhi(value));
    const dGz = formatGanZhi(dayGanZhi(jde));
    if (locale.startsWith('ko')) {
        return `${yGz}년 ${mGz}월 ${dGz}일`;
    }
    if (locale.startsWith('vi')) {
        return `Năm ${yGz} Tháng ${mGz} Ngày ${dGz}`;
    }
    return `${yGz}年 ${mGz}月 ${dGz}日`;
}
// ─── 预设格式常量 (Preset Format Patterns) ────────────────────────
/**
 * 预设格式模式常量
 *
 * 提供常用日期格式的预设模式，可直接用于 {@link format} 函数
 * 或 {@link LunarDate.format} 方法。
 *
 * @example
 * ```ts
 * const date = ChineseDate.fromGregorian(2025, 10, 11)
 * date.format(PATTERNS.GREGORIAN_CN_FULL)    // "二〇二五年十月十一日"
 * date.format(PATTERNS.GREGORIAN_CN_FULL_LOWER) // "二零二五年十月十一日"
 * date.format(PATTERNS.GREGORIAN_CN_NUMERIC) // "2025年10月11日"
 * date.format(PATTERNS.GREGORIAN_ISO)        // "2025-10-11"
 * date.format(PATTERNS.GREGORIAN_SLASH)      // "2025/10/11"
 * date.format(PATTERNS.LUNAR_FULL)           // "甲辰龙年 五月廿一"
 * date.format(PATTERNS.LUNAR_GANZHI)         // "甲辰年 丙寅月 甲子日"
 * date.format(PATTERNS.FULL_WITH_WEEKDAY)    // "二〇二五年十月十一日 星期六"
 * ```
 */
const PATTERNS = {
    /** 公历中文大写全格式: 二〇二五年十月十一日 */
    GREGORIAN_CN_FULL: '{GYcn}年{GMcn}月{GDcn}日',
    /** 公历中文小写全格式: 二零二五年十月十一日 */
    GREGORIAN_CN_FULL_LOWER: '{GYcn2}年{GMcn}月{GDcn}日',
    /** 公历数字中文格式: 2025年10月11日 */
    GREGORIAN_CN_NUMERIC: '{GY}年{GM}月{GD}日',
    /** 公历ISO格式(补零): 2025-10-11 */
    GREGORIAN_ISO: '{GY}-{GM0}-{GD0}',
    /** 公历斜杠格式: 2025/10/11 */
    GREGORIAN_SLASH: '{GY}/{GM}/{GD}',
    /** 农历全格式: 甲辰龙年 五月廿一 */
    LUNAR_FULL: '{Ygz}{Z}年 {Mname}{Dname}',
    /** 农历干支格式: 甲辰年 丙寅月 甲子日 */
    LUNAR_GANZHI: '{Ygz}年 {Mname}{Dname}',
    /** 公历中文+星期: 二〇二五年十月十一日 星期六 */
    FULL_WITH_WEEKDAY: '{GYcn}年{GMcn}月{GDcn}日 {Wname}',
    /** 公历数字+星期: 2025年10月11日 星期六 */
    NUMERIC_WITH_WEEKDAY: '{GY}年{GM}月{GD}日 {Wname}',
    /** 农历+星期: 甲辰龙年 五月廿一 星期六 */
    LUNAR_WITH_WEEKDAY: '{Ygz}{Z}年 {Mname}{Dname} {Wname}',
};
// ─── 解析 (Parsing) ──────────────────────────────────────────────
/**
 * Parse a compact format string back to LunarDateValue
 *
 * The compact format uses `/` as separator and encodes `leap` as 0/1.
 *
 * @param str - Compact format string (e.g., `"78/34/7/0/28"`)
 * @returns LunarDateValue, or `null` if the string is malformed
 *
 * @example
 * ```ts
 * parseCompact('78/34/5/0/6')   // { cycle: 78, year: 34, month: 5, leap: false, day: 6 }
 * parseCompact('78/34/2/1/15')  // { cycle: 78, year: 34, month: 2, leap: true, day: 15 }
 * parseCompact('invalid')       // null
 * ```
 */
function parseCompact(str) {
    const parts = str.split('/');
    if (parts.length !== 5)
        return null;
    const [cycle, year, month, leap, day] = parts.map(Number);
    if ([cycle, year, month, leap, day].some(isNaN))
        return null;
    return { cycle, year, month, leap: leap === 1, day };
}
/**
 * Parse an ISO format string back to LunarDateValue
 *
 * The ISO format uses `-` as separator and encodes `leap` as 0/1.
 *
 * @param str - ISO format string (e.g., `"78-34-7-0-28"`)
 * @returns LunarDateValue, or `null` if the string is malformed
 *
 * @example
 * ```ts
 * parseISO('78-34-5-0-6')   // { cycle: 78, year: 34, month: 5, leap: false, day: 6 }
 * parseISO('78-34-2-1-15')  // { cycle: 78, year: 34, month: 2, leap: true, day: 15 }
 * parseISO('invalid')       // null
 * ```
 */
function parseISO(str) {
    const parts = str.split('-');
    if (parts.length !== 5)
        return null;
    const [cycle, year, month, leap, day] = parts.map(Number);
    if ([cycle, year, month, leap, day].some(isNaN))
        return null;
    return { cycle, year, month, leap: leap === 1, day };
}
/**
 * Parse a Gregorian date string into { year, month, day }
 *
 * Supports formats:
 * - ISO: "2025-10-11"
 * - Slash: "2025/10/11"
 * - Chinese: "2025年10月11日"
 *
 * @param str - Date string
 * @returns GregorianDate or null if unparseable
 *
 * @example
 * parseGregorian('2025-10-11')  // { year: 2025, month: 10, day: 11 }
 * parseGregorian('2025/10/11')  // { year: 2025, month: 10, day: 11 }
 * parseGregorian('2025年10月11日') // { year: 2025, month: 10, day: 11 }
 */
function parseGregorian(str) {
    // ISO or slash format: YYYY-MM-DD or YYYY/MM/DD
    const isoMatch = str.match(/^(\d{4})[-/](\d{1,2})[-/](\d{1,2})$/);
    if (isoMatch) {
        const [, y, m, d] = isoMatch.map(Number);
        return { year: y, month: m, day: d };
    }
    // Chinese format: YYYY年M月D日
    const cnMatch = str.match(/^(\d{4})年(\d{1,2})月(\d{1,2})日$/);
    if (cnMatch) {
        const [, y, m, d] = cnMatch.map(Number);
        return { year: y, month: m, day: d };
    }
    return null;
}

/**
 * LunarDate — Immutable lunar date value object
 * Copyright (c) 2025 北京锋通科技有限公司 (郭玉峰, 吴琼). MIT License.
 *
 * This is the primary user-facing API. All instances are immutable —
 * any modification via {@link LunarDate.with} returns a new instance.
 *
 * Use one of the four subclasses for calendar-specific behavior:
 * - {@link ChineseDate} — Chinese calendar (UTC+8, epoch 2636 BCE)
 * - {@link JapaneseDate} — Japanese calendar (UTC+9, epoch 2636 BCE)
 * - {@link KoreanDate} — Korean calendar (UTC+8:30/9, epoch 2333 BCE)
 * - {@link VietnameseDate} — Vietnamese calendar (UTC+7/8, epoch 2636 BCE)
 *
 * @module ValueObjects
 *
 * @example
 * ```ts
 * import { ChineseDate } from 'chinese-date'
 *
 * const date = ChineseDate.fromGregorian(2024, 6, 21)
 * date.yearGanZhi          // "甲辰"
 * date.zodiac              // "龙"
 * date.weekday             // 5 (Friday)
 * date.weekdayName         // "星期五"
 * date.gregorianYearCn     // "二〇二四"
 * date.formatFull('zh-TW') // "甲辰龍年 五月初六"
 * date.format(PATTERNS.GREGORIAN_CN_FULL) // "二〇二四年六月二十一日"
 * ```
 */
// ─── Config map ───────────────────────────────────────────────────
const CONFIG_MAP = {
    chinese: CHINESE_CONFIG,
    japanese: JAPANESE_CONFIG,
    korean: KOREAN_CONFIG,
    vietnamese: VIETNAMESE_CONFIG
};
/**
 * Compute weekday (0=Sun, 6=Sat) from Gregorian date using Zeller's congruence
 * @internal
 */
function weekdayFromGregorian(year, month, day) {
    // Use JS Date for reliable weekday calculation
    return new Date(year, month - 1, day).getDay();
}
// ─── LunarDate (base class) ──────────────────────────────────────
/**
 * Base class for immutable lunar date value objects
 *
 * Stores a lunar date as cycle/year/month/leap/day and provides
 * methods for conversion, comparison, GanZhi, zodiac, festivals,
 * solar terms, formatting, weekday, and Gregorian date access.
 *
 * **Immutability:** All instances are frozen after creation.
 * Use {@link with} to create modified copies, or {@link addDays}/{@link addMonths}/{@link addYears}
 * for date arithmetic.
 *
 * **Do not instantiate `LunarDate` directly.** Use one of the subclasses:
 * - {@link ChineseDate} — Mainland China (UTC+8)
 * - {@link JapaneseDate} — Japan (UTC+9)
 * - {@link KoreanDate} — Korea (UTC+8:30/9)
 * - {@link VietnameseDate} — Vietnam (UTC+7/8)
 *
 * @example
 * ```ts
 * const date = ChineseDate.fromGregorian(2024, 6, 21)
 * date.cycle           // 78
 * date.year            // 34 (34th year in cycle 78 = 甲辰)
 * date.month           // 5
 * date.leap            // false
 * date.day             // 6
 * date.gregorianYear   // 2024
 * date.yearGanZhi      // "甲辰"
 * date.zodiac          // "龙"
 * ```
 */
class LunarDate {
    constructor(value, calendarType) {
        this.cycle = value.cycle;
        this.year = value.year;
        this.month = value.month;
        this.leap = value.leap;
        this.day = value.day;
        this.calendarType = calendarType;
        this._config = CONFIG_MAP[calendarType];
    }
    // ─── Static Constructors ─────────────────────────────────────
    /**
     * Create from a Gregorian date
     *
     * @param year - Gregorian year (e.g., 2024)
     * @param month - Month (1-12)
     * @param day - Day of month
     * @returns LunarDate instance
     *
     * @example
     * ```ts
     * LunarDate.fromGregorian(2024, 6, 21)
     * // { cycle: 78, year: 34, month: 5, leap: false, day: 6 }
     * ```
     */
    static fromGregorian(year, month, day) {
        return new LunarDate(gregorianToLunar(CHINESE_CONFIG, year, month, day), 'chinese');
    }
    /** Create from a JavaScript Date object. Uses local timezone for date components. */
    static fromDate(date) {
        return LunarDate.fromGregorian(date.getFullYear(), date.getMonth() + 1, date.getDate());
    }
    /**
     * Create from a LunarDateValue object
     *
     * Useful for reconstructing a date from serialized data.
     *
     * @param value - The lunar date value object
     * @param calendarType - Calendar variant (default: `'chinese'`)
     *
     * @example
     * ```ts
     * LunarDate.fromValue({ cycle: 78, year: 34, month: 5, leap: false, day: 6 })
     * ```
     */
    static fromValue(value, calendarType = 'chinese') {
        return new LunarDate(value, calendarType);
    }
    /** Create from JDE (Julian Ephemeris Day) */
    static fromJDE(jde) {
        const gc = new julian.CalendarGregorian().fromJDE(jde);
        return LunarDate.fromGregorian(gc.year, gc.month, Math.trunc(gc.day));
    }
    /**
     * Create from a Gregorian date string
     *
     * Supports formats:
     * - ISO: "2025-10-11"
     * - Slash: "2025/10/11"
     * - Chinese: "2025年10月11日"
     *
     * @param isoStr - Gregorian date string
     * @returns LunarDate instance, or null if the string cannot be parsed
     */
    static fromISO(isoStr) {
        const greg = parseGregorian(isoStr);
        if (!greg)
            return null;
        return LunarDate.fromGregorian(greg.year, greg.month, greg.day);
    }
    /**
     * Create from a Unix timestamp (milliseconds since 1970-01-01T00:00:00Z)
     *
     * @param ms - Unix timestamp in milliseconds
     * @returns LunarDate instance
     */
    static fromTimestamp(ms) {
        return LunarDate.fromDate(new Date(ms));
    }
    /**
     * Parse a date string and create a LunarDate instance
     *
     * Auto-detects the format:
     * - Gregorian ISO: "2025-10-11"
     * - Gregorian slash: "2025/10/11"
     * - Gregorian Chinese: "2025年10月11日"
     *
     * @param str - Date string
     * @returns LunarDate instance, or null if the string cannot be parsed
     */
    static parse(str) {
        return LunarDate.fromISO(str);
    }
    /** Get today's lunar date */
    static today() {
        return LunarDate.fromDate(new Date());
    }
    // ─── Conversion ──────────────────────────────────────────────
    /**
     * Convert to Gregorian date
     *
     * @returns Gregorian date object
     *
     * @example
     * ```ts
     * ChineseDate.fromGregorian(2024, 6, 21).toGregorian()
     * // { year: 2024, month: 6, day: 21 }
     * ```
     */
    toGregorian() {
        return lunarToGregorian(this._config, this._value());
    }
    /** Convert to JavaScript Date */
    toDate() {
        const jde = lunarToJDE(this._config, this._value());
        return new julian.CalendarGregorian().fromJDE(jde).toDate();
    }
    /** Convert to JDE */
    toJDE() {
        return lunarToJDE(this._config, this._value());
    }
    /**
     * Get the corresponding Gregorian year
     *
     * This is the most common way to get a human-readable year number.
     * Computed from `cycle` and `year` using the calendar's epoch.
     *
     * @example
     * ```ts
     * ChineseDate.fromGregorian(2024, 6, 21).gregorianYear  // 2024
     * ```
     */
    get gregorianYear() {
        return yearFromEpochCycle(this._config, this.cycle, this.year);
    }
    // ─── Weekday (星期) ─────────────────────────────────────────
    /** Weekday number (0=Sunday, 1=Monday, ..., 6=Saturday) */
    get weekday() {
        const greg = this.toGregorian();
        return weekdayFromGregorian(greg.year, greg.month, greg.day);
    }
    /** Weekday full name in simplified Chinese, e.g. "星期六" */
    get weekdayName() {
        return getWeekdayName(this.weekday, 'zh-CN');
    }
    /** Weekday short name in simplified Chinese, e.g. "六" */
    get weekdayShort() {
        return getWeekdayShort(this.weekday, 'zh-CN');
    }
    /** Get localized weekday name */
    getWeekdayName(locale = 'zh-CN') {
        return getWeekdayName(this.weekday, locale);
    }
    /** Get localized weekday short name */
    getWeekdayShort(locale = 'zh-CN') {
        return getWeekdayShort(this.weekday, locale);
    }
    // ─── Gregorian Chinese Numerals (公历中文数字) ──────────────
    /** Gregorian year in Chinese numerals (〇 style), e.g. "二〇二五" */
    get gregorianYearCn() {
        return toChineseYear(this.toGregorian().year, 'upper');
    }
    /** Gregorian year in Chinese numerals (零 style), e.g. "二零二五" */
    get gregorianYearCnLower() {
        return toChineseYear(this.toGregorian().year, 'lower');
    }
    // ─── Immutable Modification ──────────────────────────────────
    /** Return a new instance with patched fields */
    with(patch) {
        return new LunarDate({
            cycle: patch.cycle ?? this.cycle,
            year: patch.year ?? this.year,
            month: patch.month ?? this.month,
            leap: patch.leap ?? this.leap,
            day: patch.day ?? this.day
        }, this.calendarType);
    }
    // ─── Date Arithmetic ────────────────────────────────────────
    /**
     * Add days to this date, returning a new instance
     *
     * Works by converting to Gregorian, adding days, then converting back.
     * This ensures correct handling of month/year boundaries.
     *
     * @param days - Number of days to add (negative to subtract)
     * @returns New LunarDate instance
     *
     * @example
     * ```ts
     * const date = ChineseDate.fromGregorian(2024, 6, 21)
     * const tomorrow = date.addDays(1)
     * const yesterday = date.addDays(-1)
     * ```
     */
    addDays(days) {
        const greg = this.toGregorian();
        const jsDate = new Date(greg.year, greg.month - 1, greg.day + days);
        return this.constructor.fromGregorian(jsDate.getFullYear(), jsDate.getMonth() + 1, jsDate.getDate());
    }
    /**
     * Add Gregorian months to this date, returning a new instance
     *
     * Works on the Gregorian calendar: adds months and adjusts the year
     * if necessary. If the resulting day exceeds the month's length,
     * it is clamped to the last day of the month.
     *
     * @param months - Number of months to add (negative to subtract)
     * @returns New LunarDate instance
     *
     * @example
     * ```ts
     * const date = ChineseDate.fromGregorian(2024, 1, 31)
     * const next = date.addMonths(1) // 2024-02-29 (leap year, clamped)
     * ```
     */
    addMonths(months) {
        const greg = this.toGregorian();
        const jsDate = new Date(greg.year, greg.month - 1 + months, greg.day);
        return this.constructor.fromGregorian(jsDate.getFullYear(), jsDate.getMonth() + 1, jsDate.getDate());
    }
    /**
     * Add Gregorian years to this date, returning a new instance
     *
     * If the resulting date is invalid (e.g., Feb 29 on a non-leap year),
     * the day is clamped to the last day of the month.
     *
     * @param years - Number of years to add (negative to subtract)
     * @returns New LunarDate instance
     */
    addYears(years) {
        const greg = this.toGregorian();
        const jsDate = new Date(greg.year + years, greg.month - 1, greg.day);
        return this.constructor.fromGregorian(jsDate.getFullYear(), jsDate.getMonth() + 1, jsDate.getDate());
    }
    // ─── Comparison ──────────────────────────────────────────────
    /** Check equality with another LunarDate */
    equals(other) {
        return this.cycle === other.cycle &&
            this.year === other.year &&
            this.month === other.month &&
            this.leap === other.leap &&
            this.day === other.day &&
            this.calendarType === other.calendarType;
    }
    /** Check if this date is before another */
    isBefore(other) {
        return this.toJDE() < other.toJDE();
    }
    /** Check if this date is after another */
    isAfter(other) {
        return this.toJDE() > other.toJDE();
    }
    /**
     * Get the number of days between this date and another
     *
     * Returns a positive number if this date is after the other,
     * negative if before, and zero if they are the same day.
     *
     * @param other - The other date to compare with
     * @returns Number of days difference (this - other)
     *
     * @example
     * ```ts
     * const a = ChineseDate.fromGregorian(2024, 6, 21)
     * const b = ChineseDate.fromGregorian(2024, 6, 18)
     * a.diffDays(b)  // 3
     * b.diffDays(a)  // -3
     * ```
     */
    diffDays(other) {
        return Math.round(this.toJDE() - other.toJDE());
    }
    /**
     * Get the number of days in this lunar month
     *
     * A lunar month is either 29 days (small month / 小月) or 30 days (big month / 大月).
     * Calculated by converting to Gregorian and comparing the day 1 of this month
     * and the day 1 of the next month.
     *
     * @returns 29 or 30
     *
     * @example
     * ```ts
     * const date = ChineseDate.fromGregorian(2024, 6, 21)
     * date.daysInMonth()  // 29 or 30
     * ```
     */
    daysInMonth() {
        // Get the Gregorian date of this month's day 1
        const g1 = lunarToGregorian(this._config, {
            cycle: this.cycle, year: this.year, month: this.month, leap: this.leap, day: 1
        });
        // Add 29 days in Gregorian
        const g29 = new Date(g1.year, g1.month - 1, g1.day + 29);
        // Add 30 days in Gregorian
        const g30 = new Date(g1.year, g1.month - 1, g1.day + 30);
        // Convert both back to lunar
        const lunar29 = gregorianToLunar(this._config, g29.getFullYear(), g29.getMonth() + 1, g29.getDate());
        const lunar30 = gregorianToLunar(this._config, g30.getFullYear(), g30.getMonth() + 1, g30.getDate());
        // If day 30 (Gregorian + 30) is still in the same lunar month, it's a 30-day month
        const sameMonth29 = lunar29.month === this.month && lunar29.leap === this.leap &&
            lunar29.cycle === this.cycle && lunar29.year === this.year;
        const sameMonth30 = lunar30.month === this.month && lunar30.leap === this.leap &&
            lunar30.cycle === this.cycle && lunar30.year === this.year;
        // If gregorian+30 is still same month, it's 30 days
        // If gregorian+29 is still same month but gregorian+30 is not, it's 30 days
        // If gregorian+29 is already next month, it's 29 days
        if (sameMonth30)
            return 30;
        if (sameMonth29)
            return 30;
        return 29;
    }
    // ─── GanZhi (天干地支) ──────────────────────────────────────
    /** Year GanZhi pair */
    get yearGanZhiPair() {
        return yearGanZhi(this._value());
    }
    /** Month GanZhi pair */
    get monthGanZhiPair() {
        return monthGanZhi(this._value());
    }
    /** Day GanZhi pair */
    get dayGanZhiPair() {
        return dayGanZhi(this.toJDE());
    }
    /** Year GanZhi string, e.g. "甲辰" */
    get yearGanZhi() {
        return formatGanZhi(this.yearGanZhiPair);
    }
    /** Month GanZhi string, e.g. "丙寅" */
    get monthGanZhi() {
        return formatGanZhi(this.monthGanZhiPair);
    }
    /** Day GanZhi string, e.g. "甲子" */
    get dayGanZhi() {
        return formatGanZhi(this.dayGanZhiPair);
    }
    /** Full GanZhi info */
    get ganZhiInfo() {
        return {
            year: this.yearGanZhiPair,
            month: this.monthGanZhiPair,
            day: this.dayGanZhiPair
        };
    }
    // ─── Zodiac (生肖) ───────────────────────────────────────────
    /** Zodiac animal (localized) */
    getZodiac(locale = 'zh-CN') {
        return zodiac(this._value(), locale);
    }
    /** Zodiac animal in simplified Chinese */
    get zodiac() {
        return zodiac(this._value(), 'zh-CN');
    }
    /** Zodiac emoji */
    get zodiacEmoji() {
        return zodiacEmoji(this._value());
    }
    /** Zodiac index (0-11) */
    get zodiacIndex() {
        return zodiacIndex(this._value());
    }
    // ─── Festivals (节日) ────────────────────────────────────────
    /** Get festivals on this date (localized) */
    getFestivals(locale = 'zh-CN') {
        return getFestivals(this._value(), locale);
    }
    /** Get festivals on this date in simplified Chinese */
    get festivals() {
        return getFestivals(this._value(), 'zh-CN');
    }
    // ─── Solar Terms (节气) ──────────────────────────────────────
    /** Check if this date is a solar term day, returns the name or null */
    getSolarTermName(locale = 'zh-CN') {
        const greg = this.toGregorian();
        return findSolarTermOnDate(this._config, greg.year, greg.month, greg.day, locale);
    }
    // ─── Formatting ──────────────────────────────────────────────
    /**
     * Format with a custom pattern (supports all tokens including Gregorian and weekday)
     *
     * @param pattern - Format pattern string (see {@link PATTERNS} and {@link FormatToken})
     * @param locale - Locale code (default: `'zh-CN'`)
     * @returns Formatted string
     *
     * @example
     * ```ts
     * const date = ChineseDate.fromGregorian(2024, 6, 21)
     * date.format('{Ygz}{Z}年 {Mname}{Dname}', 'zh-CN')  // "甲辰龙年 五月初六"
     * date.format('{Ygz}{Z}年 {Mname}{Dname}', 'zh-TW')  // "甲辰龍年 五月初六"
     * date.format(PATTERNS.GREGORIAN_ISO)                  // "2024-06-21"
     * date.format(PATTERNS.GREGORIAN_CN_FULL)              // "二〇二四年六月二十一日"
     * ```
     */
    format(pattern, locale = 'zh-CN') {
        return format(this._value(), this.toJDE(), this.toGregorian(), pattern, locale);
    }
    /**
     * Full format with locale support
     *
     * @param locale - Locale code (default: `'zh-CN'`)
     * @returns Formatted string like `"甲辰龙年 五月初六"` (zh-CN) or `"甲辰龍年 五月初六"` (zh-TW)
     *
     * @example
     * ```ts
     * ChineseDate.fromGregorian(2024, 6, 21).formatFull()         // "甲辰龙年 五月初六"
     * ChineseDate.fromGregorian(2024, 6, 21).formatFull('zh-TW')  // "甲辰龍年 五月初六"
     * ChineseDate.fromGregorian(2024, 6, 21).formatFull('zh-HK')  // "甲辰龍年 五月初六"
     * ```
     */
    formatFull(locale = 'zh-CN') {
        return formatFull(this._value(), this.toJDE(), locale);
    }
    /**
     * Compact format: cycle/year/month/leap/day
     *
     * `leap` is encoded as 0 (false) or 1 (true).
     *
     * @returns String like `"78/34/7/0/28"`
     *
     * @example
     * ```ts
     * ChineseDate.fromGregorian(2024, 6, 21).formatCompact()  // "78/34/5/0/6"
     * ```
     */
    formatCompact() {
        return formatCompact(this._value());
    }
    /**
     * ISO format: CY-Y-M-L-D
     *
     * `leap` is encoded as 0 (false) or 1 (true).
     *
     * @returns String like `"78-34-7-0-28"`
     *
     * @example
     * ```ts
     * ChineseDate.fromGregorian(2024, 6, 21).formatISO()  // "78-34-5-0-6"
     * ```
     */
    formatISO() {
        return formatISO(this._value());
    }
    /**
     * GanZhi (干支) format: year month day pillars
     *
     * @param locale - Locale code (default: `'zh-CN'`)
     * @returns Formatted string like `"甲辰年 庚午月 甲寅日"` (zh-CN),
     *          `"甲辰년 경오월 갑인일"` (ko-KR), or `"Năm 甲辰 Tháng 庚午 Ngày 甲寅"` (vi-VN)
     *
     * @example
     * ```ts
     * ChineseDate.fromGregorian(2024, 6, 21).formatGanZhi()         // "甲辰年 庚午月 甲寅日"
     * ChineseDate.fromGregorian(2024, 6, 21).formatGanZhi('ko-KR')  // "甲辰년 경오월 갑인일"
     * ```
     */
    formatGanZhi(locale = 'zh-CN') {
        return formatGanZhiFull(this._value(), this.toJDE(), locale);
    }
    // ─── Serialization ───────────────────────────────────────────
    /** Convert to plain object */
    toJSON() {
        return {
            cycle: this.cycle,
            year: this.year,
            month: this.month,
            leap: this.leap,
            day: this.day
        };
    }
    /** Default string representation (full format) */
    toString() {
        return this.formatFull('zh-CN');
    }
    // ─── Internal ────────────────────────────────────────────────
    /** Get the raw LunarDateValue */
    _value() {
        return {
            cycle: this.cycle,
            year: this.year,
            month: this.month,
            leap: this.leap,
            day: this.day
        };
    }
}
// ─── ChineseDate ──────────────────────────────────────────────────
/**
 * Chinese calendar date (UTC+8, epoch 2636 BCE)
 *
 * The standard Chinese lunar calendar used in mainland China.
 * Uses UTC+8 timezone and the traditional epoch starting from 2636 BCE.
 *
 * **Key difference from other variants:** Uses UTC+8, which was adopted
 * in 1929. Before 1929, Beijing local solar time (UTC+7:45:40) was used.
 * This library automatically handles this historical timezone change.
 *
 * @example
 * ```ts
 * const date = ChineseDate.fromGregorian(2024, 6, 21)
 * date.yearGanZhi       // "甲辰"
 * date.zodiac           // "龙"
 * date.formatFull()     // "甲辰龙年 五月初六"
 * date.formatFull('zh-TW')  // "甲辰龍年 五月初六"
 *
 * // Static convenience methods
 * ChineseDate.newYear(2024)           // { year: 2024, month: 2, day: 10 }
 * ChineseDate.solarTerm(5, 2024)      // { year: 2024, month: 4, day: 4 } — 清明
 * ChineseDate.qingming(2024)          // { year: 2024, month: 4, day: 4 }
 * ChineseDate.festivalDate('mid-autumn', 2024)  // { year: 2024, month: 9, day: 17 }
 * ```
 */
class ChineseDate extends LunarDate {
    constructor(value) {
        super(value, 'chinese');
    }
    static fromGregorian(year, month, day) {
        return new ChineseDate(gregorianToLunar(CHINESE_CONFIG, year, month, day));
    }
    static fromDate(date) {
        return ChineseDate.fromGregorian(date.getFullYear(), date.getMonth() + 1, date.getDate());
    }
    static fromValue(value) {
        return new ChineseDate(value);
    }
    static fromJDE(jde) {
        const gc = new julian.CalendarGregorian().fromJDE(jde);
        return ChineseDate.fromGregorian(gc.year, gc.month, Math.trunc(gc.day));
    }
    /**
     * Create from a Gregorian date string
     *
     * Supports: "2025-10-11", "2025/10/11", "2025年10月11日"
     */
    static fromISO(isoStr) {
        const greg = parseGregorian(isoStr);
        if (!greg)
            return null;
        return ChineseDate.fromGregorian(greg.year, greg.month, greg.day);
    }
    /**
     * Create from a Unix timestamp (milliseconds since epoch)
     */
    static fromTimestamp(ms) {
        return ChineseDate.fromDate(new Date(ms));
    }
    /**
     * Parse a date string and create a ChineseDate instance
     *
     * Auto-detects the format:
     * - "2025-10-11", "2025/10/11", "2025年10月11日"
     */
    static parse(str) {
        return ChineseDate.fromISO(str);
    }
    static today() {
        return ChineseDate.fromDate(new Date());
    }
    /**
     * Get Chinese New Year (春节) date for a Gregorian year
     *
     * @param gyear - Gregorian year (e.g., 2024)
     * @returns Gregorian date of Spring Festival
     *
     * @example
     * ```ts
     * ChineseDate.newYear(2024)  // { year: 2024, month: 2, day: 10 }
     * ChineseDate.newYear(2025)  // { year: 2025, month: 1, day: 29 }
     * ```
     */
    static newYear(gyear) {
        const jde = newYear(CHINESE_CONFIG, gyear);
        const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
        return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
    }
    /**
     * Get a specific solar term's Gregorian date
     *
     * @param term - Solar term number (1-24). 1=立春, 5=清明, 24=大寒
     * @param gyear - Gregorian year
     * @returns Gregorian date of the solar term
     *
     * @example
     * ```ts
     * ChineseDate.solarTerm(5, 2024)   // { year: 2024, month: 4, day: 4 } — 清明
     * ChineseDate.solarTerm(1, 2024)   // { year: 2024, month: 2, day: 4 } — 立春
     * ```
     */
    static solarTerm(term, gyear) {
        return getSolarTerm(CHINESE_CONFIG, term, gyear);
    }
    /** Get all 24 solar terms in a year */
    static solarTermsInYear(gyear, locale = 'zh-CN') {
        return getSolarTermsInYear(CHINESE_CONFIG, gyear, locale);
    }
    /**
     * Get Qingming (清明) date — the 5th solar term, typically April 4 or 5
     *
     * Culturally significant as the date for 扫墓 (tomb-sweeping).
     *
     * @param gyear - Gregorian year
     * @returns Gregorian date of Qingming
     *
     * @example
     * ```ts
     * ChineseDate.qingming(2024)  // { year: 2024, month: 4, day: 4 }
     * ChineseDate.qingming(2025)  // { year: 2025, month: 4, day: 4 }
     * ```
     */
    static qingming(gyear) {
        return getQingming(CHINESE_CONFIG, gyear);
    }
    /**
     * Get a festival's Gregorian date by key
     *
     * @param key - Festival key (e.g., `'spring'`, `'mid-autumn'`, `'dragon-boat'`, `'new-year-eve'`)
     * @param gyear - Gregorian year
     * @returns Gregorian date, or `null` if the key is not found
     *
     * @example
     * ```ts
     * ChineseDate.festivalDate('spring', 2024)       // { year: 2024, month: 2, day: 10 }
     * ChineseDate.festivalDate('mid-autumn', 2024)   // { year: 2024, month: 9, day: 17 }
     * ChineseDate.festivalDate('dragon-boat', 2024)  // { year: 2024, month: 6, day: 10 }
     * ChineseDate.festivalDate('new-year-eve', 2024) // { year: 2025, month: 1, day: 28 }
     * ```
     */
    static festivalDate(key, gyear) {
        return getFestivalDate(CHINESE_CONFIG, key, gyear);
    }
    /** Get all festivals in a year */
    static festivalsInYear(gyear, locale = 'zh-CN') {
        return getFestivalsInYear(CHINESE_CONFIG, gyear, locale);
    }
    with(patch) {
        return new ChineseDate({
            cycle: patch.cycle ?? this.cycle,
            year: patch.year ?? this.year,
            month: patch.month ?? this.month,
            leap: patch.leap ?? this.leap,
            day: patch.day ?? this.day
        });
    }
}
// ─── JapaneseDate ─────────────────────────────────────────────────
/**
 * Japanese calendar date (UTC+9, epoch 2636 BCE)
 *
 * Uses the same epoch as the Chinese calendar but with UTC+9 timezone.
 * The timezone difference can cause different leap month assignments
 * in rare cases where a new moon falls near midnight.
 *
 * Japanese month names use traditional names (睦月, 如月, 弥生, etc.)
 * when formatted with `'ja-JP'` locale.
 *
 * @example
 * ```ts
 * const date = JapaneseDate.fromGregorian(2024, 6, 15)
 * date.formatFull('ja-JP')  // "甲辰年 皐月初十"
 * date.getZodiac('ja-JP')   // "辰"
 * ```
 */
class JapaneseDate extends LunarDate {
    constructor(value) {
        super(value, 'japanese');
    }
    static fromGregorian(year, month, day) {
        return new JapaneseDate(gregorianToLunar(JAPANESE_CONFIG, year, month, day));
    }
    static fromDate(date) {
        return JapaneseDate.fromGregorian(date.getFullYear(), date.getMonth() + 1, date.getDate());
    }
    static fromValue(value) {
        return new JapaneseDate(value);
    }
    static fromISO(isoStr) {
        const greg = parseGregorian(isoStr);
        if (!greg)
            return null;
        return JapaneseDate.fromGregorian(greg.year, greg.month, greg.day);
    }
    static fromTimestamp(ms) {
        return JapaneseDate.fromDate(new Date(ms));
    }
    static parse(str) {
        return JapaneseDate.fromISO(str);
    }
    static today() {
        return JapaneseDate.fromDate(new Date());
    }
    /** Get Japanese New Year for a Gregorian year */
    static newYear(gyear) {
        const jde = newYear(JAPANESE_CONFIG, gyear);
        const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
        return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
    }
    with(patch) {
        return new JapaneseDate({
            cycle: patch.cycle ?? this.cycle,
            year: patch.year ?? this.year,
            month: patch.month ?? this.month,
            leap: patch.leap ?? this.leap,
            day: patch.day ?? this.day
        });
    }
}
// ─── KoreanDate ───────────────────────────────────────────────────
/**
 * Korean calendar date (UTC+8:30/9, epoch 2333 BCE — Dangun era)
 *
 * Uses the Dangun (단군) epoch starting from 2333 BCE, which means
 * the Korean `cycle`/`year` values differ from Chinese for the same
 * Gregorian date. Korea used UTC+8:30 before 1912 and UTC+9 after.
 *
 * **Important:** Because the epoch differs, `KoreanDate.cycle` and
 * `KoreanDate.year` will NOT match `ChineseDate.cycle` and `ChineseDate.year`
 * for the same Gregorian date. Use `gregorianYear` for comparison.
 *
 * @example
 * ```ts
 * const date = KoreanDate.fromGregorian(2024, 6, 15)
 * date.formatFull('ko-KR')  // "辛丑년 오월10일"
 * date.gregorianYear        // 2024
 * date.getZodiac('ko-KR')   // "진(龍)"
 * ```
 */
class KoreanDate extends LunarDate {
    constructor(value) {
        super(value, 'korean');
    }
    static fromGregorian(year, month, day) {
        return new KoreanDate(gregorianToLunar(KOREAN_CONFIG, year, month, day));
    }
    static fromDate(date) {
        return KoreanDate.fromGregorian(date.getFullYear(), date.getMonth() + 1, date.getDate());
    }
    static fromValue(value) {
        return new KoreanDate(value);
    }
    static fromISO(isoStr) {
        const greg = parseGregorian(isoStr);
        if (!greg)
            return null;
        return KoreanDate.fromGregorian(greg.year, greg.month, greg.day);
    }
    static fromTimestamp(ms) {
        return KoreanDate.fromDate(new Date(ms));
    }
    static parse(str) {
        return KoreanDate.fromISO(str);
    }
    static today() {
        return KoreanDate.fromDate(new Date());
    }
    /** Get Korean New Year for a Gregorian year */
    static newYear(gyear) {
        const jde = newYear(KOREAN_CONFIG, gyear);
        const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
        return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
    }
    with(patch) {
        return new KoreanDate({
            cycle: patch.cycle ?? this.cycle,
            year: patch.year ?? this.year,
            month: patch.month ?? this.month,
            leap: patch.leap ?? this.leap,
            day: patch.day ?? this.day
        });
    }
}
// ─── VietnameseDate ───────────────────────────────────────────────
/**
 * Vietnamese calendar date (UTC+7/8, epoch 2636 BCE)
 *
 * Uses the same epoch as the Chinese calendar but with UTC+7 timezone
 * (before 1968) or UTC+8 (after 1968). The timezone difference can
 * cause different leap month assignments compared to the Chinese calendar.
 *
 * @example
 * ```ts
 * const date = VietnameseDate.fromGregorian(2024, 6, 15)
 * date.formatFull('vi-VN')  // "Năm 甲辰 Tháng 5Ngày 10"
 * date.getZodiac('vi-VN')   // "Thìn"
 * ```
 */
class VietnameseDate extends LunarDate {
    constructor(value) {
        super(value, 'vietnamese');
    }
    static fromGregorian(year, month, day) {
        return new VietnameseDate(gregorianToLunar(VIETNAMESE_CONFIG, year, month, day));
    }
    static fromDate(date) {
        return VietnameseDate.fromGregorian(date.getFullYear(), date.getMonth() + 1, date.getDate());
    }
    static fromValue(value) {
        return new VietnameseDate(value);
    }
    static fromISO(isoStr) {
        const greg = parseGregorian(isoStr);
        if (!greg)
            return null;
        return VietnameseDate.fromGregorian(greg.year, greg.month, greg.day);
    }
    static fromTimestamp(ms) {
        return VietnameseDate.fromDate(new Date(ms));
    }
    static parse(str) {
        return VietnameseDate.fromISO(str);
    }
    static today() {
        return VietnameseDate.fromDate(new Date());
    }
    /** Get Vietnamese New Year for a Gregorian year */
    static newYear(gyear) {
        const jde = newYear(VIETNAMESE_CONFIG, gyear);
        const gc = new julian.CalendarGregorian().fromJDE(jde + 0.5);
        return { year: gc.year, month: gc.month, day: Math.trunc(gc.day) };
    }
    with(patch) {
        return new VietnameseDate({
            cycle: patch.cycle ?? this.cycle,
            year: patch.year ?? this.year,
            month: patch.month ?? this.month,
            leap: patch.leap ?? this.leap,
            day: patch.day ?? this.day
        });
    }
}

export { CHINESE_CONFIG, ChineseDate, GAN$1 as GAN, JAPANESE_CONFIG, JapaneseDate, KOREAN_CONFIG, KoreanDate, GAN as LOCALE_GAN, SOLAR_TERMS as LOCALE_SOLAR_TERMS, ZHI as LOCALE_ZHI, ZODIAC as LOCALE_ZODIAC, LunarDate, PATTERNS, VIETNAMESE_CONFIG, VietnameseDate, ZHI$1 as ZHI, allGanZhi, availableLocales, dayGanZhi, dayGanZhiStr, festivalKeys, findSolarTermOnDate, format, formatCompact, formatFull, formatGanZhi, formatGanZhiFull, formatISO, fromJDE, ganChar, ganZhiToCycleIndex, getConfig, getFestivalDate, getFestivals, getFestivalsInYear, getGanZhiInfo, getLocale, getMajorSolarTerm, getMinorSolarTerm, getQingming, getSolarTerm, getSolarTermJDE, getSolarTermsInYear, getWeekdayName, getWeekdayShort, gregorianToLunar, inMajorSolarTerm, isLeapMonth, isLocaleSupported, lunarToGregorian, lunarToJDE, majorSolarTermJDE, midnight, minorSolarTermJDE, monthGanZhi, monthGanZhiStr, newYear, nextNewMoon, parseCompact, parseGanZhi, parseGregorian, parseISO, previousNewMoon, solarTermJDE, solarTermName, solarTermNames, toChineseNumber, toChineseYear, yearFromEpochCycle, yearGanZhi, yearGanZhiStr, zhiChar, zodiac, zodiacEmoji, zodiacFromGregorianYear, zodiacIndex, zodiacList };
